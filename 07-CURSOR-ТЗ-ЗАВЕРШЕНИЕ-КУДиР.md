# ТЗ завершения КУДиР (Cursor)

Дата сверки: 06.09.2026. Код и документы 00–06 **не менялись**; этот файл только фиксирует фактическое состояние и очередь работ.

Правила приоритета (как в `00-АРХИТЕКТУРА-КУДиР.md`): при противоречии специализированное ТЗ важнее архитектуры, плана №3, аудита №6 и устаревших статусов `pending` внутри №5. Matching — ТЗ №2. Состав фактов и CSV — ТЗ №1. Память выгрузки — ТЗ №4.

Состояния пункта:

| Метка | Смысл |
| --- | --- |
| `CLOSED_IN_SPEC` | Правило уже в актуальном ТЗ; переписывать формулировку нечего, пока нет нового противоречия в коде. |
| `CLOSED_IN_CODE` | Поведение есть в текущем исходнике. |
| `TESTED` | Есть автоматический тест, который именно это проверяет и сейчас зелёный. |
| `NEEDS_LIVE_1C` | Без прогона в информационной базе 1С 7.7 пункт нельзя считать принятым. |
| `STILL_OPEN` | В актуальном ТЗ требование есть, а реализации и/или правильного теста нет. |

Несколько меток у одного пункта допустимы: например код закрыт тестом исходника, но не прогнан в базе.

Методика оценки готовности — **не процент**, а покрытие четырёх слоёв: ТЗ → код → автотест → живая 1С. Процент не выводится, потому что веса слоёв не заданы договором приёмки.

Автотесты на момент сверки: `python -m unittest discover -s tests -q` → **103 OK, 1 skipped** (живой HTTP-parser).

---

## 1. Оценка текущего состояния

Проект **не** на старте matcher и **не** на старте выгрузки. Архитектура «один цикл», схема CSV v2, пакет `src/kudir/` и модуль `1cv77/kudir_export.txt` (метка `2026.09.05-03 этап6-stream`) уже существуют. Matching golden 2.1–2.9 и последующие ревью-кейсы зелёные.

Книга КУДиР **не** готова к сдаче по этому комплекту:

1. налогового слоя `src/kudir/tax.py` нет; `kudir.py` пишет технические нули НДС и `income = amount`;
2. три правила аудита №6, которые уже вписаны в ТЗ, в matcher/налоге ещё расходятся с кодом или не реализованы: расход состава (B2), gap (B6), provisional-долг (B5);
3. живой прогон 1С 7.7 (ОСВ, полный цикл, битая ссылка) не зафиксирован;
4. отрицательные 50/51/55→62 выгружаются как обычная оплата, а Python-загрузчик отвергает любой знак минус в копейках.

Устаревшие документы **не** являются текущим планом работ:

- `замечание-текущие.txt` — архив до заморозки ТЗ (второй цикл, FIFO, фиктивное сальдо прототипа). Этапы 0–4.6 плана №3 это закрыли; заново не открывать.
- Раздел «Разрыв: текущий код vs ТЗ» в №5 — снимок на `2026.09.04-07` / `этап5-verify`. Он противоречит шапке того же №5 (`2026.09.05-03`), тестам исходника и самому `kudir_export.txt`.
- Построчные `pending` у чекпоинтов 6.1–6.9 в №5 **не** доказательство, что поток RAM не сделан: заголовки этапов, тесты `test_kudir_stage60_contract.py` / `test_kudir_export_source.py` / `test_kudir_loader_source.py` и код говорят об обратном. Не переоткрывать 6.1–6.9 без нового расхождения код↔ТЗ.
- Формула «P0/P1 = 0» в старых кусках №3 уже снята самим №3 для выгрузки/налога; matcher 4.6 не переоткрывать.

Три независимых «version», их нельзя сливать:

| Что | Где | Значение сейчас | Зачем |
| --- | --- | --- | --- |
| CSV `schema_version` | `manifest.csv`, `csv_io.SCHEMA_VERSION` | `"2"` | колонки обмена, в т.ч. три агрегата НДС |
| `scoring.yaml` `schema_version` | `config/scoring.yaml` | `1` | набор ключей весов/порогов matcher |
| пакет Python | `src/kudir/__init__.py` | `0.4.6` | ревизия matcher, не схема CSV |

Доказательство раздельности: `engine.py` пишет в `run_status.csv` поле `schema_version` из `csv_io.SCHEMA_VERSION` (`"2"`), а содержимое YAML учитывается отдельно хешем `scoring_hash`. `ScoringConfig.schema_version` в `run_status` не попадает. Совпадение цифр «1» у YAML и старого CSV v1 — случайность истории, не контракт.

---

## 2. Фактически завершённые блоки

Не переоткрывать без нового конкретного расхождения.

### 2.1. ТЗ и канон

- Один цикл, три окна, разрез DEBT/ADVANCE по аналитике ledger, VAT-шкалы аванса и реализации, `MatchHorizonEnd`, `SUCCESS` ≠ `tax_ready`, FIFO нет — в 00 / 01 / 02 / 04.
- Источник НДС аванса по состоянию налогового окна; поздняя реализация не отменяет ранний счёт — в 00 §5.4, 01 §16/§27, 02 §21.2 (закрытие формулировки B1).
- Расход состава, история, превышение счёта, округление `Окр` — 02 §21.2 (формулировка B2).
- Формат неизвестного НДС и `vat_unknown_count` — 01 §24, 02 §21.2 (формулировка B3).
- Документ после `ДатаКон` — обход доступности без `signed_balance` — 00 §5.2, 02 §4 (формулировка B4).
- Provisional-долг §27.1 — 02 (формулировка B5).
- Доказательный gap между лучшим и вторым — 02 §31 (формулировка B6).
- Граница подписанных 50/51/55→62 — 01 §12.1 (формулировка B7).
- Этапы плана №3 **0–4.6** как история matching: ТЗ заморожено, golden matching, пакет `src/kudir/`, не `stub_match`.

### 2.2. Python matching (без налога)

- `src/kudir/`: `ledger.py`, `matcher.py`, `scoring.py`, `combinations.py`, `engine.py`, `loaders.py` (v2), parser-адаптер, диагностика.
- `csv_io.py`: `SCHEMA_VERSION = "2"`, три агрегата в `DOCUMENTS_FIELDS`, `state_uncertain` / `historical_unresolved_*` в `RUN_STATUS_KEYS`. Автоконвертации v1→v2 нет. Чекпоинт №5 **6.10.4** закрыт 06.09.2026.
- Golden matching в `tests/golden/` (список в `test_golden_matcher.py` / `test_golden_schema.py`), включая `december_advance_january_shipment` (документы после `date_end`).
- Запуск: `python -m kudir`, `run_kudir.bat`. Прототип `kudir_proto` / `run_kudir_proto.bat` — транспорт, не рабочий matcher.

### 2.3. Выгрузка и загрузчик 1С в исходнике

Модуль `1cv77/kudir_export.txt` `2026.09.05-03`: проход A → `run_id` → B → поток ledger/documents/сальдо; `schema_version;2`; агрегаты НДС; `ЗначениеВСтрокуВнутр`; только PAYMENT/DOCUMENT/CONTRACT; L1/L2/L3; сальдо режим 1 + `ВключатьСубсчета(-1)`; `КУДиР_Копейки` через `Окр` без `+ 0.001`; ПКО по `Врег` обеих сторон; CONTRACT до очистки полей оплаты.

Это **закрыто тестами исходника**, не живой базой.

### 2.4. Что сознательно не входит в версию 1

Semantic reranker не обязателен. Возвраты денег и эквайринг как полный учёт — вне скоупа (01 §12.1). Нарезка P2/P3 памяти — только если живой прогон всё ещё валит `Операция`. Parser в репозиторий не копируется.

---

## 3. Только реально открытые задачи

Ниже нет пунктов, которые уже закрыты более новой редакцией ТЗ **и** кодом **и** тестом, кроме пометки `NEEDS_LIVE_1C` там, где живая база ещё не подтверждала факт.

### T1. Налоговый слой `tax.py`

**Актуальное ТЗ:** 02 §1, §21.2, §40, критерии §49 п.23/27/28/32; 00 §5.4; 01 §27. Файл `src/kudir/tax.py` прямо назван следующим шагом во введении ТЗ №2.

**Сейчас:** файла нет. `result_row()` в `kudir.py` всегда `vat_*=0`, `income_kopecks=amount`. `engine.py` считает `tax_ready` только по `DEBT_UNRESOLVED` / исторической неопределённости, без состава НДС. Matcher не хранит `invoice_id` для `INVOICE_VAT_FALLBACK`. Golden matching с нулевым НДС — ожидаемое поведение **цикла matching**, не доказательство налога. `pre_vat_advance_post_vat_shipment` имеет `tax_ready=1` при нулевом НДС: после `tax.py` такие expected нужно пересмотреть, а не считать эталоном УСН.

**Тесты:** налоговых golden нет (`early_invoice_late_shipment` есть в списке ТЗ №2 §48, каталога в `tests/golden/` нет). `RUN_STATUS_KEYS` **не** содержит `vat_unknown_count`, хотя 01 §24 и 02 §2.1 его требуют.

**Чего нет:** модуль расхода состава; выбор источника по налоговому окну; 5/105 только как шаг 3; `VAT_COMPOSITION_UNKNOWN`; счётчик; тексты «без НДС» / «в т.ч. НДС 5%» по подтверждённому составу; согласование `tax_ready` с налогом.

### T2. Gap HIGH (B6) — ТЗ уже новое, код и тесты старые

**ТЗ:** 02 §31: gap = лучший минус **второй** допустимый; A=180, B=180, C=100 → не HIGH.

**Код:** `pick_high` в `scoring.py`: при нескольких равных лучших `second` берётся из **остальных** (слабый C). Тогда gap = 180−100 и классификатор даёт HIGH, хронология выбирает ближайшего.

**Тесты, которые закрепляют ошибку (сейчас зелёные):**

- `tests/test_audit_46.py` → `test_tied_group_separated_from_rest_uses_nearest`
- `tests/test_matcher_review.py` → аналогичный кейс с C

`test_two_equal_without_rest_are_not_high` закрывает только пару без третьего — это **не** приёмка B6.

Пункт 4.6.4 плана №3 описывает старое правило («равные, отделённые от остальных порогом gap»). Приоритет — ТЗ №2 §31, не 4.6.4.

**Чего нет:** исправленный `pick_high`; красный тест B6; правка двух тестов, которые сейчас требуют запрещённое HIGH.

### T3. Provisional-долг и комбинация нескольких оплат (B5)

**ТЗ:** 02 §27.1 и критерий §49 п.30; контрольный случай 5 500 + 27 200 vs оплаты 4 500, 27 200, 1 000.

**Код:** `_match_debt` при отсутствии HIGH сразу пишет `DEBT_UNRESOLVED` (`_record_unresolved`). Bounded search в `combinations.py` — **одна** оплата на несколько документов, не несколько оплат на один документ. Структуры provisional нет.

**Тесты:** golden на 4 500 / 27 200 / 1 000 нет.

**Чего нет:** отложенные долговые части внутри контрагента до timeout/cap; объединение с поздней 1 000; окончательный UNRESOLVED только в конце контрагента.

### T4. Подписанные оплаты 50/51/55→62 и отрицательный НДС ТЧ (B7)

**ТЗ:** 01 §12.1, §21, §27; 02 §21.2 (отрицательный НДС ТЧ → неизвестный состав). Алгоритм DEBT/ADVANCE — на неотрицательную часть.

**1С:** отрицательная сумма попадает в `payments.csv` тем же проходом B (`коп = КУДиР_Копейки(Опер.Сумма)`), без отдельной диагностики «не штатное поступление» и без запрета объявлять книгу готовой. Отрицательный НДС строки ТЧ логируется, строка **не** идёт в облагаемые/необлагаемые, но агрегаты **не обнуляются**: в CSV уходят суммы только положительных строк. Это не «агрегаты не утверждать как налоговый факт».

**Python:** `_kop` в `loaders.py` требует `raw.isdigit()` — минус даёт `FAILED` всего запуска. Это не диагностика версии 1 и не `tax_ready=0` при сохранённом matching.

**Тесты:** нет.

**Чего нет:** явное распознавание сторно поступления vs возврат vs штатный плюс; signed kopecks во входе Python; `tax_ready=0` / diagnostic event; нулевые/неизвестные агрегаты при отриц. НДС.

### T5. Память `invoice_id` для налога (следствие B1, не дыра matching 2.4)

**ТЗ:** 02 §21.2: matcher помнит счёт, даже если RECEIVABLE нет или он только в extended window.

**Код:** в `src/kudir` нет поля `invoice_id` у результата/аванса.

**Тесты:** нет. Golden 2.4 проверяет хозяйственный `ADVANCE_EXTENDED_MATCH`, не налог по мартовскому счёту.

**Чего нет:** стабильная ссылка на допустимый счёт для `tax.py`.

### T6. Контракт `run_status.csv`: `vat_unknown_count`

**ТЗ:** 01 §24; 02 §2.1 и §49 п.32.

**Код:** ключа нет в `csv_io.RUN_STATUS_KEYS` и в `engine.py`.

**Тесты:** `test_golden_schema.py` сверяет ключи с `RUN_STATUS_KEYS` — сейчас это закрепляет **отсутствие** поля.

### T7. Живая 1С: факты A1–A6, эталон A7, полный цикл 6.10.1–6.10.3

**ТЗ:** 01 §23.1/§6/§21/§23.3/§24/§28/§30; 04 §13–§15; №5 этап 6.10 и чек-лист 5.9.7.

**Код:** исправления A1–A6 в исходнике есть.

**Тесты:** только статический разбор `kudir_export.txt` (103 теста включают эти контракты). Поиск подстроки не доказывает API `БухгалтерскиеИтоги` на живых итогах.

**Чего нет:** сверка ОСВ; проводка+сторно = 0 в базе; живой ПКО; CONTRACT A/B без реализации; отброс таблицы на плохом числе; логическая запись `id_map` с настоящей `ЗначениеВСтрокуВнутр`; полный SUCCESS 1С→Python→L3; битая ссылка.

### T8. Согласование статусов в №5 (документация, не код)

Построчные `pending` 6.1–6.9 расходятся с заголовками тех же этапов и с кодом. Это путает очередь и провоцирует переоткрытие. Исправлять **после** или параллельно с реализацией T1–T7, не вместо них. Существующие ТЗ 00–06 в этой сессии не трогать — отдельный чекпоинт, когда пользователь разрешит правку документов.

### Не открывать как отдельные блокеры

| Тема | Почему не открывать |
| --- | --- |
| Второй цикл НДС | Отменён каноном «один цикл». |
| FIFO / закрытие аванса после всех оплат | Закрыто в ТЗ и matcher (golden 2.1, 2.8). |
| Фиктивное сальдо прототипа, лимит 20 оплат, дедуп ledger по document_id | Не в `kudir_export.txt`. |
| `schema_version` YAML = 2 | Не требуется: это другой контракт. |
| Переписать поток EXPORT «потому что в №5 pending» | Код потока уже в модуле 2026.09.05-03. |
| `kudir_proto` как основа matcher | Прямо запрещено №3 / №2. |

---

## 4. Чекпоинты завершения (строго по порядку)

Зависимости: не начинать живой «налоговый» прогон книги, пока T1 не даёт ненулевой осмысленный НДС на golden. Не чинить поток RAM заново. Matcher 4.x не переписывать целиком: точечные T2/T3/T5.

### Чекпоинт C0. Зафиксировать очередь (этот файл)

- **Цель:** один актуальный план без переоткрытия 0–4.6 и 6.1–6.9.
- **Файлы:** `07-CURSOR-ТЗ-ЗАВЕРШЕНИЕ-КУДиР.md` (готово).
- **Изменения:** нет кода.
- **Готово, когда:** команда реализации идёт по C1…C7, а не по аудиту №6 «с нуля» и не по устаревшему «Разрыв» №5.
- **Тесты:** не требуются.
- **Зависимости:** нет.

### Чекпоинт C1. Gap B6 в `pick_high`

- **Цель:** HIGH только при отрыве лучшего от второго; слабый третий не развязывает равную пару.
- **Файлы:** `src/kudir/scoring.py`; `tests/test_audit_46.py`; `tests/test_matcher_review.py`; при необходимости `tests/golden/ambiguous_equal_shipments`.
- **Изменения:** при `len(tied) > 1` считать `second = best_score` (gap 0), а не score слабого rest; хронологию не вызывать для создания HIGH. Переписать два теста, которые сейчас ждут HIGH на A=B плюс C. Добавить явный тест из §31 (180/180/100 → `None`).
- **Готово, когда:** пример §31 не даёт HIGH; пара без третьего по-прежнему не HIGH; уникальный лидер с gap к второму по-прежнему HIGH.
- **Тесты:** unit `pick_high`; прогон golden matching без регрессии 2.1/2.8.
- **Зависимости:** нет (ТЗ уже CLOSED_IN_SPEC). Не ждать `tax.py`.

### Чекпоинт C2. Provisional DEBT (B5)

- **Цель:** кейс 4 500 → 27 200 → 1 000 связывает 4 500+1 000 с 5 500 и 27 200 с 27 200, не выбивая остаток ранним UNRESOLVED.
- **Файлы:** `src/kudir/matcher.py`; возможно `combinations.py`; новый `tests/golden/multi_payment_debt_combo/` (имя не догма).
- **Изменения:** неоднозначный долг внутри контрагента не эмитит `DEBT_UNRESOLVED` и не занимает remainder, пока жив deadline контрагента; search объединяет provisional с поздними оплатами; HIGH и разрез DEBT/ADVANCE на дату оплаты не пересматривать; по концу контрагента / cap / timeout — UNRESOLVED без занятия remainder.
- **Готово, когда:** контрольный случай §27.1 зелёный; уже существующие HIGH-golden не ломаются.
- **Тесты:** новый golden + текущий `test_golden_matcher.py`.
- **Зависимости:** логично после C1 (тот же scoring/gap на отложенных кандидатах), но не после налога.

### Чекпоинт C3. Контракт налога в CSV (`vat_unknown_count`, агрегаты уже v2)

- **Цель:** выходы Python соответствуют ТЗ №1/№2 **до** или **вместе** с первой строкой `tax.py`.
- **Файлы:** `src/kudir_proto/csv_io.py` (`RUN_STATUS_KEYS`); `src/kudir/engine.py`; `tests/test_golden_schema.py`; пересборка golden `expected_run_status.csv` через `tests/golden/build_golden.py`.
- **Изменения:** добавить `vat_unknown_count` в канон ключей; matching без `tax.py` пишет `0`. Не менять `SCHEMA_VERSION` CSV и не трогать `scoring.yaml` `schema_version: 1`.
- **Готово, когда:** схема golden и `write_kv` содержат поле; 6.10.4 не регрессирует.
- **Тесты:** `test_golden_schema.py`, matcher golden.
- **Зависимости:** нет. Можно параллельно с C1/C2.

### Чекпоинт C4. `tax.py` + память счёта (B1, B2, B3, T5)

- **Цель:** тот же запуск matcher считает УСН/НДС по §21.2.
- **Файлы:** новый `src/kudir/tax.py`; `src/kudir/engine.py`; `src/kudir/matcher.py` (invoice на авансе); `src/kudir/kudir.py` (тексты содержания после известного НДС); golden `tests/golden/early_invoice_late_shipment` и налоговые expected к 2.4/2.5/partial_advance.
- **Изменения:**
  - расход `remaining_*` по timeline, включая `is_target=0`;
  - источник аванса: RECEIVABLE в налоговом окне → иначе счёт с `invoice_date <= payment_tax_quarter_end` → иначе 5/105 или 0;
  - extended RECEIVABLE не источник и не отмена счёта;
  - превышение ёмкости счёта — отдельная часть;
  - округление как `Окр` 1С 7.7, хвост копеек последней части;
  - неизвестный состав: нули + `VAT_COMPOSITION_UNKNOWN` + `tax_ready=0`, не писать «без НДС»;
  - `DEBT_UNRESOLVED` без угадывания НДС.
- **Готово, когда:** пример B1 (март счёт смешанный, апрель отгрузка) даёт НДС 3 000 / доход 102 000, добавление апрельской отгрузки налог не меняет; квартал vs год на одном комплекте фактов совпадают по целевому НДС; превышение счёта 150 000 vs 105 000 не пропорционирует сверх ёмкости.
- **Тесты:** `early_invoice_late_shipment`; расход состава (история, три частичных, округление); unit округления. Matching golden: обновить только налоговые колонки и `tax_ready`, не факты связей.
- **Зависимости:** C3 (ключ run_status). Желательно C1, чтобы налог не утверждал ложный HIGH. T5 входит сюда. **Не** зависит от живой 1С.

### Чекпоинт C5. B7: знак оплаты и неизвестный состав при отриц. НДС

- **Цель:** минус в 50/51/55→62 и минус НДС ТЧ не выглядят как готовая КУДиР.
- **Файлы:** `1cv77/kudir_export.txt`; `src/kudir/loaders.py`; `src/kudir/matcher.py` / `tax.py`; тесты исходника + Python.
- **Изменения (1С):** не считать отрицательную денежную корреспонденцию штатным `is_target`-доходом; лог + признак для Python либо нулевой вклад в готовую книгу. При `отрицНДС=1` не писать частичные агрегаты как факт: нули, как при пустой ТЧ / расхождении. **(Python):** принимать signed копейки ledger/payments; не разлагать отрицательную часть как обычный DEBT/ADVANCE; diagnostic + `tax_ready=0` / FAILED по правилу, которое нужно явно выбрать в реализации **в рамках уже написанного 01 §12.1** (диагностика, не полный учёт возвратов). Не расширять версию 1 до возвратов.
- **Готово, когда:** −0,01 руб. в ledger = −1 коп. и доходит до Python; целевая отрицательная оплата не даёт `tax_ready=1`; отриц. НДС ТЧ → неизвестный состав.
- **Тесты:** исходник (обнуление агрегатов при флаге); Python signed `_kop`; golden/unit на отказ штатного разложения.
- **Зависимости:** C4, чтобы неизвестный состав уже имел формат строки. Живая проверка знака — C6.

### Чекпоинт C6. Живая приёмка 1С (A1–A7, 6.10.1–6.10.3)

- **Цель:** хотя бы одна полная цепочка SUCCESS на валидных данных и отдельные FAILED; сверка ОСВ, не old CSV как единственный эталон.
- **Файлы:** живая база; `exchange/<run_id>/`; лог 1С; чек-лист 5.9.7 в №5 (как инструкция приёмки, без обязательной правки файла в этой сессии).
- **Изменения:** только если живой прогон найдёт расхождение API (индексы субконто, вид ПКО метаданных, сериализация ссылки). Не планировать заранее перестройку потока.
- **Готово, когда:** пункты 1–6 и 14 списка §7 аудита №6 выполнены **в базе**; 6.10.2 — полный цикл, не «дошли до FAILED из‑за Python»; 6.10.3 — битая ссылка, пустая таблица, процесс жив.
- **Тесты:** ручные + выгрузка, прогнанная тем же `python -m kudir` / `run_kudir.bat`.
- **Зависимости:** для **налоговой** книги — C4. Для проверки фактов A1–A6 и памяти выгрузки Python matching (даже с нулевым НДС) достаточен **после** C1/C2, если не оценивать УСН. Имеет смысл: сначала факты+matching на базе, затем повтор с `tax.py`.

### Чекпоинт C7. Документальная гигиена (когда разрешат править 00–06)

- **Цель:** не держать ложные `pending` в №5 и «не стартовать разработку» в №3 как актуальный приказ.
- **Файлы:** `05-…`, выборочно статусная шапка `03-…`. **Не** раздувать ТЗ №1/№2 без новой ошибки.
- **Готово, когда:** чекпоинт 6.x, уже закрытый кодом и тестом исходника, помечен «реализовано / тест исходника / не проверено в 1С» единообразно.
- **Тесты:** нет.
- **Зависимости:** после C6 или по отдельной команде. **Сейчас не делать** — пользователь запретил менять существующие ТЗ в этой сессии.

---

## 5. Аудит №6: A1–A7 и B1–B7 на 06.09.2026

Аудит от 05.09.2026 не видел Python-пакет, `scoring.yaml` и тесты. Ниже — повторная сверка с **текущими** файлами репозитория.

### A1. Сальдо: аналитика и субсчета

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (исходник) + `NEEDS_LIVE_1C`
- **Доказательство:** 01 §23.1; `КУДиР_СобратьСальдоКлиента` — режимы 2/1/1, `ВключатьСубсчета(-1)` до запроса; `test_5_9_1_opening_balance_mode_1_and_subaccounts`. Режим 3 в активном коде сальдо отсутствует.
- **Не закрыто:** ОСВ в базе (индексы `ВыбратьСубконто` vs фактический запрос).

### A2. Копейки сторно

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (исходник) + `NEEDS_LIVE_1C`
- **Доказательство:** `КУДиР_Копейки` = `Цел(Окр(Число(Сумма) * 100, 0))`, без `+ 0.001`; `test_5_9_2_kopecks_preserve_sign`.
- **Не закрыто:** проводка + сторно = 0 на живых проводках.

### A3. ПКО

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (исходник) + `NEEDS_LIVE_1C`
- **Доказательство:** `КУДиР_ВидОплаты` сравнивает `Врег(СокрЛП(ВидДок))` с `Врег("ПКО")` / `ПриходныйОрдер` / `ПриходныйКассовыйОрдер`; нет `Найти(Врег(ВидДок), "ПриходныйОрдер")`; `test_5_9_3_pko_normalized_kind`.
- **Не закрыто:** фактический идентификатор вида в конкретной ИБ (если метаданные другие — только живая проверка это покажет).

### A4. Полнота CONTRACT

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (исходник) + `NEEDS_LIVE_1C`
- **Доказательство:** регистрация CONTRACT в проходе B **до** очистки договора; также ledger и documents; `test_5_9_4_contract_registered_per_payment_and_ledger`.
- **Не закрыто:** приёмка «один payment_id, договоры A/B, нет реализации, оба CONTRACT в id_map, L3 грузит обе части» в базе.

### A5. Число в загрузчике

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (исходник) + `NEEDS_LIVE_1C`
- **Доказательство:** `КУДиР_ЭтоЦелыеКопейки`, инварианты сумм, `глОшибка`, нет `Возврат 0` в активном `КУДиР_КопейкиВРубли`; `test_5_9_5_numeric_error_discards_whole_table`.
- **Не закрыто:** живой файл с пустым НДС / буквой после валидных строк.

### A6. Логическая CSV-запись L2 и тип объекта

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (исходник) + `NEEDS_LIVE_1C`
- **Доказательство:** `КУДиР_ПрочитатьЛогическуюCSVСтроку`, `ЗначениеИзСтрокиВнутр`, проверка «неверного типа»; `test_5_9_6_restore_from_id_map_logical_csv`.
- **Не закрыто:** настоящая системная строка с CR/LF, незакрытая кавычка, чужой тип на живой платформе.

### A7. Эталон приёмки ≠ старая выгрузка

- **Статус:** `CLOSED_IN_SPEC` + `STILL_OPEN` (исполнение) + `NEEDS_LIVE_1C`
- **Доказательство закрытия в ТЗ/плане:** чек-лист допустимых расхождений 5.9.7 в №5.
- **Код:** не применим как отдельная функция.
- **Тесты:** нет живого эталона ОСВ.
- **Не закрыто:** сама приёмка 6.10.1.

### B1. Поздняя реализация vs ранний счёт

- **Статус:** `CLOSED_IN_SPEC` + `STILL_OPEN` (код/тест налога)
- **ТЗ:** 00, 01, 02 согласованы: налог по окну, extended не отменяет счёт. Пример 10.03 счёт / 15.03 аванс / 20.04 отгрузка — НДС 3 000.
- **Код:** `tax.py` нет; нули в `kudir.py` не реализуют правило.
- **Тесты:** каталога `early_invoice_late_shipment` нет. Golden 2.4 проверяет matching, НДС там 0.
- **Не делать:** снова править формулировку приоритета в ТЗ, пока C4 не покажет иное.

### B2. Расход состава документа/счёта

- **Статус:** `CLOSED_IN_SPEC` + `STILL_OPEN` (код/тест)
- **ТЗ:** 02 §21.2 полностью описывает remaining_*, историю, превышение счёта, `Окр`, последнюю часть.
- **Код/тесты:** нет `tax.py`, нет приёмочных golden расхода.

### B3. Формат неизвестного НДС и run_status

- **Статус:** `CLOSED_IN_SPEC` + `STILL_OPEN` (код: семантический формат и счётчик)
- **ТЗ:** технические нули + `VAT_COMPOSITION_UNKNOWN` + `tax_ready=0`; `vat_unknown_count`; `state_uncertain` / `historical_unresolved_*` уже в №1 и №2.
- **Код:** нули в каждой строке matching **без** reason `VAT_COMPOSITION_UNKNOWN` — это заглушка matching, не закрытие B3. `state_uncertain` пишется (`historical_unresolved_blocks_tax`, TESTED). `vat_unknown_count` отсутствует.
- **Не переоткрывать** как «не заданы нули в ТЗ»: в ТЗ заданы.

### B4. Документы после `ДатаКон`

- **Статус:** `CLOSED_IN_SPEC` + `CLOSED_IN_CODE` + `TESTED` (Python matching) + `NEEDS_LIVE_1C` (выгрузка горизонта)
- **ТЗ:** отдельный ввод RECEIVABLE из `documents.csv` без ledger-события и без `signed_balance`.
- **Код:** все строки `documents.csv` регистрируются в `Ledger.register_document` до обхода ledger; `_try_high_close` зовётся только с ledger-события реализации; **хвост аванса** закрывается `_conflict_pass` / `_resolve_open_advance` по `ledger.documents` с отсечкой `match_horizon_end`, без изменения баланса от самой строки документа. После `ДатаКон` в запуске нет следующих оплат — отложенное HIGH в pass эквивалентно «сразу при появлении» для остатков этого запуска. Отдельного имени функции «обход доступности» нет; поведение критерия приёмки есть.
- **Тесты:** `tests/golden/december_advance_january_shipment`: `date_end=2025-12-31`, отгрузка 2026-01-15 только в documents, expected `ADVANCE_EXTENDED_MATCH` / `EXTENDED_MATCH_NO_TAX_RECALC_Q4`.
- **Не переоткрывать** как отсутствие механизма, если не появится кейс, где conflict pass не видит документ без ledger. Налог этой связи — C4, не B4.

### B5. Комбинация нескольких долговых оплат

- **Статус:** `CLOSED_IN_SPEC` + `STILL_OPEN` (код/тест)
- **См. открытую задачу T3.** Аудит просил проверить Python после появления файлов: файлы есть, provisional нет.

### B6. Gap и слабый третий

- **Статус:** `CLOSED_IN_SPEC` + `STILL_OPEN` (код) ; автотесты **противоречат** ТЗ
- **См. T2.** Этап 4.6.4 закрывал **другое** правило. Новое правило §31 важнее. Зелёные тесты 4.6 здесь не доказательство закрытия B6.

### B7. Граница версии 1 для знака 62 и НДС ТЧ

- **Статус:** `CLOSED_IN_SPEC` + частично `CLOSED_IN_CODE` (лог отриц. НДС; `Окр` копеек) + `STILL_OPEN` (штатная отрицательная оплата, частичные агрегаты, Python `isdigit`) + `NEEDS_LIVE_1C`
- **См. T4.** Сторно в ledger выгружать точно — A2/копейки; это не замена правила «не штатное поступление».

---

## 6. Особые проверки (сводка)

### `tax.py`

Файла нет. Налог в том же запуске **не** реализован. Заглушка нулей в `kudir.py` / golden matching не считается налоговым слоем и не закрывает B1–B3.

### Документы после `ДатаКон`

Matching: закрыто кодом+golden 2.4 (B4). Выгрузка до `MatchHorizonEnd`: в исходнике 1С есть (`КУДиР_СобратьДокументы`, `МесяцевГоризонта`) — `TESTED` исходник, `NEEDS_LIVE_1C`. Налог extended без отмены счёта — не закрыто (`tax.py`).

### Provisional / комбинации оплат

ТЗ §27.1 есть. Реализация — однооплатный combination search. Кейс 4 500 / 27 200 / 1 000 открыт.

### Gap rule

ТЗ исправлено. `pick_high` и два unit-теста живут по старому правилу «отрыв пары от C». Открыто.

### Отрицательные 50/51/55→62

ТЗ 01 §12.1 есть. 1С пишет сумму со знаком в ту же таблицу оплат. Python не читает знак. Открыто.

### `schema_version` CSV vs scoring.yaml

Это **разные** версии. CSV v2 доказан `csv_io.py`, манифестом golden, `kudir_export.txt` (`schema_version;2`), тестом `test_6_0_1_schema_version_2`. YAML v1 — ключи весов matcher. Сводить их к одному числу нельзя и не нужно для закрытия 6.10.4.

---

## 7. Рекомендуемый порядок реализации (когда будет команда писать код)

```text
C1 gap
C2 provisional DEBT     (можно почти параллельно с C1 после общего понимания scoring)
C3 vat_unknown_count
C4 tax.py + invoice_id + golden налога
C5 знак 50/51/55 и отриц. НДС ТЧ
C6 живая 1С (факты можно начинать после C1/C2; налоговую книгу — после C4)
C7 правка статусов в №3/№5 — только по отдельному разрешению
```

Существующий код и ТЗ 00–06 в сессии, породившей этот файл, не изменялись.
