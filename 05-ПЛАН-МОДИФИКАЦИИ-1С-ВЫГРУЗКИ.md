> **Исторический документ — статус уточнён 13.09.2026.**
>
> Это план предыдущего этапа. Его очередность, статусы и формулировки «рабочий план», «новый канон» или «следующий этап» относятся к моменту составления; план не следует выполнять буквально по новым требованиям. Новый план реализации составляется отдельно после согласования актуальной методики.
>
> Актуальное чтение: [общий процесс и работа бухгалтера — документ 10](10-МЕТОДИЧЕСКОЕ-ТЗ-СОПОСТАВЛЕНИЕ-ОПЛАТ.md), затем [методика функции — документ 11](11-МЕТОДИЧЕСКОЕ-ТЗ-ФУНКЦИИ-СОПОСТАВЛЕНИЯ.md) и [текстовые сценарии](MATCHING-TEST-CASES.md). Требования к будущей реализации: [ТЗ №1, §31 — обмен](01-ТЗ-1С-выгрузка-данных.md), [ТЗ №2, §51 — Python](02-ТЗ-Python-анализ-оплат.md), [ТЗ №4, §19 — повторная выгрузка](04-ТЗ-1С-память-выгрузки.md). Действующие налоговые правила и возвраты читаются в ТЗ №2 с учётом его раздела о приоритете редакций; документ 11 их не заменяет. Неутверждённые предложения §14.2 документа 11 сохраняют свой статус. Обновление требований не означает, что код уже реализован.
>
> Ниже сохранён исходный текст; прежние обозначения актуальности относятся к его собственной редакции.

# План модификации `1cv77/kudir_export.txt`

Уточнение статуса от 12.09.2026: этот план сохраняется как история предыдущего технического этапа. Для будущего обмена приняты замечания, отражённые в ТЗ №1 §31, №2 §51 и №4 §19. Шаги ниже, требующие payments.csv/is_target, старого горизонта или неизменности прежней схемы, новым требованиям не соответствуют и не подлежат буквальному выполнению. Реализация и обновление исполняемых тестов — только по отдельному запросу; в текущем этапе изменены исключительно документы. Новая методика — документ 11, редакция 4, с предложениями §14.2 и принятым правилом §14.3 о передаче неопределённостей бухгалтеру.

Рабочий план следующего этапа реализации. Не замена ТЗ.

## Ревизия 06.09.2026

Этап **6.10.4** (Python schema v2): `SCHEMA_VERSION = "2"`, три агрегата НДС в `DOCUMENTS_FIELDS`, golden пересобраны, автоконвертации v1→v2 нет. Живая приёмка 6.10.1–6.10.3 и расчёт НДС (`tax.py`, B1–B3) — ещё открыты.

Предыдущая ревизия 05.09.2026 по аудиту №6:

Источник: `06-АУДИТ-ТЗ-КУДиР-2026-09-05.md`. Направление (поток, EXPORT/LOAD, `ссылка_1С`, агрегаты НДС, schema v2) сохраняется. Меняется очередь: **сначала исправить исходные факты и загрузчик, потом потоковую перестройку RAM**.

Иначе успешно завершившийся запуск может отдать неверные остатки или суммы. Потоковые чекпоинты 6.0–6.10 не стартуют, пока не закрыты **A1–A6**. Сравнение со старой выгрузкой не является единственным эталоном приёмки (A7).

Запрет «не переписывать уже проверенные формулы сальдо» **не** распространяется на исправление настройки `БухгалтерскиеИтоги`, перевода в копейки и распознавания ПКО: текущее поведение здесь ошибочно относительно ТЗ №1.

Противоречия Python-налога B1–B7 закрываются в `00` / `01` / `02`; этот файл фиксирует только следствия для выгрузки 1С (CONTRACT, числа, CSV L2, приёмка).

Источники требований:

| Документ | Что задаёт для этого файла |
| --- | --- |
| `01-ТЗ-1С-выгрузка-данных.md` | состав фактов, периоды, ID, CSV-протокол, агрегаты НДС, загрузка результата |
| `02-ТЗ-Python-анализ-оплат.md` | контракт входа Python (`schema_version=2`, колонки `documents.csv`); matching в 1С не переносится |
| `03-ПЛАН-ИСПРАВЛЕНИЙ-КУДиР.md` | канон «один цикл»; этапы 0–4.6 закрыты; следующий шаг — выгрузка агрегатов + память + L1/L2/L3 |
| `04-ТЗ-1С-память-выгрузки.md` | потоковая запись, Dictionary, `registeredIds`/`restore`, `ссылка_1С`, освобождение RAM до Python |

Объект работ — только `1cv77/kudir_export.txt` и его контрактные тесты исходника. Python-matcher, `tax.py` и golden v2 — соседние работы; этот план фиксирует, какой контракт 1С им обязана отдать.

**Не наследовать** `1cv77/kudir_proto.txt`. Не переписывать уже проверенные формулы ID, периоды, обход `Счет` / `РасходнаяНакладная` / `ОказаниеУслуг`. Механизм сальдо остаётся `БухгалтерскиеИтоги`, но параметры запроса, перевод в копейки и вид ПКО — исправить по этапу 5.9.

---

## Цель

Текущий `КУДиР_Выполнить()` собирает все факты в RAM, пишет CSV одной большой строкой, держит живые объекты в `глТаблID` и восстанавливает их тем же словарём после Python. На живой базе это аварийно обрывает процесс. Одновременно в файле нет агрегатов НДС табличной части, `schema_version` всё ещё `1`, а `ссылка_1С` — `СокрЛП(Объект)`.

Нужно:

1. выгрузить **тот же набор фактов**, что требует ТЗ №1, плюс три агрегата НДС в `documents.csv`;
2. не держать в RAM полные ledger / documents / id_map и полный текст CSV;
3. писать `ссылка_1С = ЗначениеВСтрокуВнутр(Объект)` и восстанавливать `ЗначениеИзСтрокиВнутр()`;
4. после Python строить узкий `restore` проходами L1/L2/L3, а не тащить `глТаблID` через matching;
5. дойти до полного комплекта CSV, Python, `run_status.csv` и `СформироватьТаблицуДоходовЧистовая()` на периоде, где текущая версия падает.

Успех — контролируемый `SUCCESS` / `SUCCESS_DEGRADED` / `FAILED` с логом, не исчезновение процесса 1С 7.7.

---

## Что нельзя менять

1. Состав выборки: все оплаты истории анализируемых клиентов, `is_target`, все 62 без дедупа, документы до `MatchHorizonEnd`, обязательный `opening_balances.csv`.
2. Имена и порядок колонок, `;`, Windows-1251, даты `YYYY-MM-DD`, копейки, формулы ID.
3. Matching, FIFO, поиск строки выписки по сумме, обрезка номера до цифр — в 1С не переносить.
4. Не сужать `МесяцевИстории` / `МесяцевГоризонта` ради памяти.
5. Не глотать ошибки, не подставлять `0` / `Проведен=1`, не возвращать частичный результат.
6. Оборванный CSV = `FAILED`; Python на таком каталоге не запускать.
7. Календарную нарезку (P2 ТЗ №4) и нарезку по клиентам (P3) в эту версию не включать заранее.
8. Исключение: **обязательно** исправить настройки `БухгалтерскиеИтоги`, перевод в копейки и распознавание ПКО (этап 5.9). Это не смена состава фактов, а устранение неверного API/арифметики.

При противоречии: состав фактов — ТЗ №1; жизненный цикл памяти и `ссылка_1С` — ТЗ №4; алгоритм matching — ТЗ №2 (этот файл его не реализует).

---

## Разрыв: текущий код vs ТЗ

Зафиксировано по `1cv77/kudir_export.txt` на дату плана (`version: 2026.09.04-07`, метка `этап5-verify`).

### Память и порядок `КУДиР_Выполнить` (ТЗ №4)

| Сейчас | Должно быть |
| --- | --- |
| Сначала полные `таблОплаты` + `таблLedger` + `таблДокументы` + `таблСальдо` + `глТаблID`, потом `run_id` и запись | Проход A (клиенты) → `run_id` → потоковая запись фактов |
| CSV: `Текст = Текст + строка`, затем один `Write` | `TextStream`: заголовок, затем построчный `Write`/`WriteLine`, буфер не больше 50–100 строк |
| `глТаблID` хранит живые объекты, в том числе на каждую строку ledger | `registeredIds`: только `PAYMENT`/`DOCUMENT`/`CONTRACT` → `СистСсылка`; ledger-объект не удерживать |
| Исторические оплаты всех клиентов копятся, потом чужие удаляются | Сначала целевые клиенты, история только по ним |
| Линейные `КУДиР_НайтиОплату`, `КУДиР_КлиентВВыборке`, `КУДиР_НайтиСсылку` | `Scripting.Dictionary` |
| `глЛогТекст` копится целиком и копируется в каталог запуска | Писать сразу в файл; прогресс ledger ~500, документы ~200 |
| Структуры выгрузки живут до и после Python | Освободить до `КУДиР_ЗапуститьPython()` |

### Контракт данных (ТЗ №1 + канон «один цикл»)

| Сейчас | Должно быть |
| --- | --- |
| `ИспользоватьСубконто(Договоры/ВидыРасчетов, , 3)` — аналитика исключена; нет `ВключатьСубсчета` | отбор клиента режимом 2, разворот договора и вида расчётов режимом 1, субсчета 62 без родительского итога |
| `КУДиР_Копейки`: `Цел(Сумма * 100 + 0.001)` | знак сохраняется; −100,00 → −10000; −0,01 → −1 |
| `КУДиР_ВидОплаты` ищет `"ПриходныйОрдер"` в `Врег(ВидДок)` | точный идентификатор ПКО этой конфигурации, обе стороны одинаково нормализованы |
| Договор оплаты регистрируется до агрегации (строка ~864) | это свойство **сохранить** при перестройке: каждый непустой CONTRACT корреспонденции оплаты + документы |
| `schema_version;1` | `schema_version;2` |
| В `documents.csv` нет `СуммаОблагаемаяКоп` / `СуммаНеоблагаемаяКоп` / `СуммаНДСКоп` | Три агрегата в том же обходе документа |
| Табличная часть не обходится | `ВыбратьСтроки` / `ПолучитьСтроку` по колонке `НДС`; строки сразу забыть |
| `КУДиР_ВидРасчетовИзПроводок62` закомментирован | Для проведённого `RECEIVABLE` без реквизита — проводки 62; ни там ни там → `FAILED` |
| `id_map`: `COUNTERPARTY` и `LEDGER` пишутся | Только `PAYMENT`, `DOCUMENT`, `CONTRACT` |
| `ссылка_1С = СокрЛП(глТаблID.ссылка)` | `ЗначениеВСтрокуВнутр(Объект)`, без `СокрЛП()` |
| Загрузчик ищет объект в `глТаблID` | L1 множества ID → L2 `restore` из файла → L3 `ЗначениеИзСтрокиВнутр()` |
| `КУДиР_КопейкиВРубли` ставит `глОшибка=1` и возвращает 0; загрузчик смотрит только `Ошибок` | любая ошибка числа/ID/типа → полностью пустая таблица; `глОшибка` учитывается |
| CSV: одна физическая строка, лишние поля допустимы | логическая CSV-запись для L2; точное число полей; незакрытые кавычки = FAILED |
| Сообщение «цикл 2 не стартовать» | Устарело: одного цикла достаточно; `tax_ready=0` не блокирует загрузку |

### Контрактные тесты исходника (поправить вместе с кодом)

`tests/test_kudir_export_source.py` и `tests/test_kudir_loader_source.py` описывают этап 3/5 и schema v1. Часть ожиданий уже расходится с ТЗ после аудита:

- тест 3.3 ищет `"ОтгрузкаТоваров"` и `ЖурналДокументов`; в коде и ТЗ №1 §14 вид — `РасходнаяНакладная`;
- тест вида расчётов требует вызов `КУДиР_ВидРасчетовИзПроводок62` внутри `КУДиР_ДобавитьДокумент` — вызов закомментирован;
- `test_no_silent_try_except` ждёт тексты «в конфигурации отсутствует» / «нет реквизита суммы», которых в текущих функциях нет;
- загрузчик проверяет фразу «цикл 2 не стартовать».

Тесты исходника обновляются **в том же этапе**, что и код: сначала новый контракт (красные), затем реализация (зелёные). Не ослаблять проверки «нет FIFO / нет лимита 20 оплат / нет пустого `Попытка/Исключение`».

---

## Целевой порядок `КУДиР_Выполнить`

Канон ТЗ №4 §12, с агрегатами НДС в том же обходе документов:

```text
инициализация (пути, лог в файл, МесяцевИстории/Горизонта)
→ проход A: множество КонтрагентID целевых клиентов за ДатаНач…ДатаКон (50/51/55 → 62)
→ если клиентов нет: пустая СформироватьТаблицуДоходовЧистовая(), Python не вызывать
→ run_id + exchange/<run_id>/
→ manifest.csv (schema_version=2)
→ открыть id_map.csv (заголовок)
→ проход B: история оплат только выбранных клиентов → агрегат в RAM по payment_id
→ payments.csv + регистрация PAYMENT в id_map
→ ledger62.csv потоково (уникальность ledger_event_id, без LEDGER в id_map)
→ documents.csv потоково: шапка + агрегаты НДС, регистрация DOCUMENT/CONTRACT
→ opening_balances.csv
→ закрыть потоки, export SUCCESS
→ обнулить таблицы, Dictionary, COM этапа EXPORT (включая registeredIds)
→ Python
→ run_status.csv
→ L1: kudir_result.csv → множества нужных ID
→ L2: id_map.csv → узкий restore
→ L3: kudir_result.csv → ЗначениеИзСтрокиВнутр() → итоговая таблица
→ СформироватьТаблицуДоходовЧистовая()
```

P0 (потоковый CSV) и P1 (архитектура RAM) делаются **одним патчем конечной архитектуры**, не двумя промежуточными версиями.

---

## Структуры RAM

`registeredIds != restore`. Разное время жизни, разные имена, разные объекты.

| Имя | Время жизни | Назначение |
| --- | --- | --- |
| `clients` | EXPORT, проходы A–сальдо | `КонтрагентID` → ссылка контрагента (нужна сальдо) |
| `payments` | EXPORT, проход B | `payment_id` → номер строки компактной `таблОплаты` |
| `paymentIds` | EXPORT, ledger | экспортированные `payment_id` (связка ledger↔оплата) |
| `documents` | EXPORT, обход документов | дедуп `document_id` |
| `ledgerIds` | EXPORT, ledger | уникальность `ledger_event_id` (без объекта 1С) |
| `registeredIds` | EXPORT | ключ `entity_type\|id` → `СистСсылка` для `PAYMENT`/`DOCUMENT`/`CONTRACT` |
| `restore` | LOAD, после Python, L2–L3 | те же ключи, только ID из `kudir_result.csv` |

После экспорта `registeredIds` уничтожается **до** Python и не переиспользуется как `restore`.

Полные `таблLedger`, `таблДокументы`, старая `глТаблID` с колонкой живой `ссылка` — запрещены как постоянное хранилище. Компактная `таблОплаты` допустима до конца прохода B (сумма и справочные договор/вид расчётов могут донакапливаться). После записи `payments.csv` широкие поля очистить, оставить `paymentIds`.

---

## Этапы и чекпоинты

Статусы: `pending` — не начато. Закрывать чекпоинт только когда выполнен критерий «Готово, когда».

Сводка:

| Этап | Чекпоинтов | Суть |
| --- | --- | --- |
| **5.9 Факты до потока** | **7** | A1 сальдо, A2 копейки, A3 ПКО, A4 CONTRACT, A5 числа, A6 CSV L2, A7 эталон приёмки |
| 6.0 Контракт тестов | 3 | Красные тесты под v2 / память / L1–L3 / факты 5.9 |
| 6.1 Потоки CSV | 4 | P0: нет полного файла в переменной |
| 6.2 ID и ссылка_1С | 5 | `registeredIds`, три типа, `ЗначениеВСтрокуВнутр` |
| 6.3 Проход A + run_id | 3 | Клиенты до каталога запуска |
| 6.4 Проход B + payments | 4 | История только выбранных; CONTRACT оплат сохранить |
| 6.5 Потоковый ledger | 4 | Нет `таблLedger`, нет `LEDGER` в `id_map` |
| 6.6 Документы + НДС | 6 | Агрегаты, вид расчётов, schema v2 |
| 6.7 Сальдо и индексы | 3 | Dictionary; запрос сальдо — как в 5.9.1 |
| 6.8 Лог и разделение пиков | 3 | Файл-лог, освобождение до Python |
| 6.9 Загрузчик L1/L2/L3 | 5 | `restore` после Python; A5/A6 обязательны |
| 6.10 Приёмка | 4 | Независимые ожидания A1–A3 + полный цикл |
| **Всего** | **51** | |

Этап 5.9 блокирует 6.1–6.10. Schema v2 Python (**6.10.4**) закрыт 06.09.2026; соседние работы: `tax.py` по ТЗ №2 после B1–B3.

---

## Этап 5.9. Исправление исходных фактов до потоковой перестройки

**Статус:** исходник 2026.09.05-03 закрыл A1–A6 и поток 6.0–6.9 тестами исходника; живая 1С, A7/ОСВ и Python schema v2 — этап 6.10.

Закрывает A1–A7 аудита №6. A1–A6 закрыты в исходнике; живая сверка A7 — этап 6.10.

### 5.9.1. A1 — начальное сальдо: аналитика и субсчета

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-01 / проверено тестом исходника / не проверено в 1С
- **ТЗ:** №1 §23.1
- **Готово, когда:** `ИспользоватьСубконто`: контрагент — режим **2** (отбор), договоры и виды расчётов — режим **1** (разворот). Вызван `ВключатьСубсчета` так, чтобы субсчета 62 были в детализации, а родительский итог 62 не дублировал их. Индексы `ВыбратьСубконто` совпадают с фактическим составом запроса. Остатки — непосредственно перед `ИсторияНач`, без повторного включения движения первого дня. Неттинг между договорами/субсчетами запрещён.

**Приёмка:** один клиент, два договора, два субсчета 62; одновременные Дт и Кт; нулевое сальдо третьей аналитики в файл не попадает. Сверка с ОСВ.

### 5.9.2. A2 — перевод в копейки сохраняет знак сторно

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-01 / проверено тестом исходника / не проверено в 1С
- **ТЗ:** №1 §21
- **Готово, когда:** −100,00 → −10000; −0,01 → −1; +100,00 → 10000. Односторонняя добавка `+ 0.001` перед `Цел` удалена. Исключение возвратов из версии 1 не разрешает портить отрицательные движения ledger.

**Приёмка:** проводка и равное сторно дают сумму 0; −0,01 руб. = −1 копейка.

### 5.9.3. A3 — ПКО по точному виду документа

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-01 / проверено тестом исходника / не проверено в 1С
- **ТЗ:** №1 §6
- **Готово, когда:** обе стороны сравнения нормализованы одинаково (`Врег` с обеих сторон или равенство идентификатору метаданных). Не искать `"ПриходныйОрдер"` внутри уже верхнего регистра. Фактический идентификатор ПКО этой конфигурации зафиксировать в комментарии модуля / тесте исходника. Это смена распознавания типа, не формул ID.

**Приёмка:** ПКО с несколькими проводками 50→62 → одна CASH-оплата от позиции; разные банковские проводки одной выписки → разные `payment_id`.

### 5.9.4. A4 — полнота CONTRACT до Python

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-01 / проверено тестом исходника / не проверено в 1С
- **ТЗ:** №1 §23.3, №2 §24 и §40, №4 §7.4
- **Готово, когда:** каждый непустой договор каждой экспортируемой корреспонденции оплаты зарегистрирован в `id_map` **до** очистки справочных полей `payments`. Договоры документов тоже. Перед Python в `id_map` есть все `CONTRACT`, из которых Python имеет право заполнить `contract_id` (включая незакрытый ADVANCE из аналитики ledger62).

**Приёмка:** один `payment_id`, договоры A и B, нет реализации/счёта по B — оба CONTRACT в `id_map`; L1/L2/L3 загружают обе части.

### 5.9.5. A5 — ошибка числа отбрасывает весь результат

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-02 / проверено тестом исходника / не проверено в 1С
- **ТЗ:** №1 §24 и §28, №4 §13
- **Готово, когда:** L1 валидирует все числовые поля как целые копейки. Пустое поле ≠ 0. Нечисловая строка, дубль `result_row_id`, нарушение `advance + repayment = amount`, `income = amount − vat`, `0 <= vat <= vat_base <= amount` (для поддерживаемых неотрицательных частей) → единый отказ, полностью пустая таблица. `глОшибка=1` учитывается перед возвратом. Для сверки сумм с `payments.csv` после освобождения EXPORT читать с диска компактный набор ID/сумм, не держать широкую таблицу до Python.

**Приёмка:** валидная строка, затем пустой НДС / буква вместо копеек / неверное разложение / дубль `result_row_id` — пустой результат и контролируемая ошибка.

### 5.9.6. A6 — логическая CSV-запись для L2 и проверка типа объекта

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-02 / проверено тестом исходника / не проверено в 1С
- **ТЗ:** №1 §30, №4 §13.2
- **Готово, когда:** чтение `id_map.csv` собирает логическую запись: переносы внутри кавычек, удвоенные кавычки, точное число полей, EOF внутри кавычек = FAILED, лишнее поле = FAILED. `СокрЛП` системной строки запрещён. После `ЗначениеИзСтрокиВнутр()` проверяется, что восстановлено агрегатное значение нужного типа, а не «любое непустое». Указание плана №3 «не писать parser логических записей» относится только к однострочному `kudir_result.csv` и не запрещает корректный L2.

Либо явно сузить формат `id_map` (запрет CR/LF в полях) **и** доказать это на живой сериализации; молчаливый `ReadLine` недостаточен.

**Приёмка:** перенос/кавычки в допустимом поле; незакрытая кавычка; лишнее поле; корректная сериализация неверного типа; две разные ссылки с разными ID. Ошибки — процесс жив, таблица пуста.

### 5.9.7. A7 — эталон приёмки не равен старой выгрузке

- **Статус:** чек-лист зафиксирован; сверка с ОСВ — 6.10.1
- **ТЗ:** №4 §15.1
- **Готово, когда:** в чек-листе 6.10 перечислены допустимые расхождения со старым CSV: ключи/суммы сальдо (A1), знаки копеек (A2), группировка ПКО (A3), заполненный вид расчётов RECEIVABLE, три агрегата НДС, только три типа в `id_map`, системная `ссылка_1С`. Остальные бизнес-поля — полное совпадение. Обязательна сверка с ОСВ/проводками контрольной базы, не только old/new. Поиск подстроки в исходнике не доказывает правильный параметр API.

Допустимые расхождения со старым CSV при приёмке 6.10.1:

1. Ключи и суммы `opening_balances.csv` — режим субконто 1 и субсчета 62 (A1).
2. Знак сторно в копейках ledger/payments/сальдо (A2).
3. Группировка ПКО: одна CASH-оплата на документ, не BANK по проводке (A3).
4. `ВидРасчетовID` проведённого RECEIVABLE из реквизита или проводок 62.
5. Три агрегата НДС сразу после `СуммаКоп` в `documents.csv`.
6. `manifest.schema_version=2`.
7. В `id_map.csv` только `PAYMENT` / `DOCUMENT` / `CONTRACT`; нет `COUNTERPARTY` и `LEDGER`.
8. `ссылка_1С` — `ЗначениеВСтрокуВнутр()`, не `СокрЛП(Объект)`.
9. CONTRACT каждой корреспонденции оплаты, в том числе аванс без реализации.

---

## Этап 6.0. Контракт исходника до кода

**Статус:** реализовано / 39 тестов исходника зелёные на 2026.09.05-03

Сначала тесты. Реализация без красного контракта снова разъедется с ТЗ.

### 6.0.1. Обновить `tests/test_kudir_export_source.py` под schema v2 и ТЗ №4

- **Статус:** реализовано в `tests/test_kudir_stage60_contract.py` + `tests/test_kudir_export_source.py`
- **Готово, когда:** тесты требуют `schema_version;2`; заголовок `documents.csv` с тремя агрегатами; `ЗначениеВСтрокуВнутр`; нет `COUNTERPARTY`/`LEDGER` в регистрации `id_map`; нет накопления `Текст = Текст +` для ledger/id_map; вид документа `РасходнаяНакладная`, не `ОтгрузкаТоваров`; `ЖурналДокументов` не обязателен; режим субконто сальдо не `3` для договоров/видов расчётов; нет одностороннего `+ 0.001` в копейках; ПКО не ищется смешанным регистром внутри `Врег`.

Пока `csv_io.py` остаётся v1, тест заголовка `documents.csv` должен явно ждать новые колонки либо обновляться вместе с Python-контрактом в отдельном коротком коммите. Не оставлять `SCHEMA_VERSION = "1"` как канон выгрузки.

### 6.0.2. Обновить `tests/test_kudir_loader_source.py` под L1/L2/L3

- **Статус:** реализовано в `tests/test_kudir_loader_source.py` + `tests/test_kudir_stage60_contract.py`
- **Готово, когда:** загрузчик не содержит `КУДиР_НайтиСсылку` как способ восстановить объект; есть `ЗначениеИзСтрокиВнутр`; два прохода по `kudir_result.csv` и один по `id_map.csv`; нет «цикл 2 не стартовать»; `tax_ready=0` по-прежнему не делает `Возврат` до чтения строк; частичная таблица по ошибке ID отбрасывается; учитывается `глОшибка`; числовые поля валидируются в L1; после десериализации проверяется тип объекта.

### 6.0.3. Зафиксировать запреты в тестах

- **Статус:** реализовано (запреты FIFO / `глМаксОплат` / пустой `Попытка` / `document_lines.csv` / второй обход «за НДС»)
- **Готово, когда:** по-прежнему запрещены FIFO, `глМаксОплат`, пустой `Попытка/Исключение`, `СокрЛП(` вокруг системной ссылки в записи `id_map`, `document_lines.csv`, второй обход документов «за НДС».

---

## Этап 6.1. Потоковая запись CSV (P0)

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.1.1. Хелперы потока

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** есть открытие `TextStream` (заголовок), запись строки через существующее `КУДиР_ПолеCSV()`, закрытие; полный файл в одной переменной `Текст` для `payments`/`ledger62`/`documents`/`opening_balances`/`id_map` отсутствует. `manifest.csv` можно оставить малой строкой.

Рекомендуемые имена (не догма): `КУДиР_ОткрытьCSV`, `КУДиР_ПисатьCSVСтроку`, `КУДиР_ЗакрытьCSV`. Буфер 50–100 строк допустим, рост пропорционально всему файлу — нет.

### 6.1.2. Незавершённая выгрузка

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** сбой после создания каталога ставит `глОшибка = 1`, закрывает открытые потоки, **не** запускает Python, каталог **не** удаляет, в лог пишутся этап, файл и число готовых строк.

`export_status.csv` (`IN_PROGRESS|SUCCESS|FAILED`) рекомендуется, но не обязан менять Python-ТЗ.

### 6.1.3. CSV-протокол без регрессии

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** `;`, удвоение кавычек, кавычки вокруг `;` / `"` / CR/LF, пустое поле пустое. `ЗначениеВСтрокуВнутр()` сразу в `КУДиР_ПолеCSV()`, без `СокрЛП()`.

### 6.1.4. Удалить `КУДиР_ЗаписатьВыгрузку` как сборщик пяти файлов из таблиц

- **Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** запись каждого большого CSV происходит в своём проходе, а не из полных `таблLedger`/`таблДокументы`/`глТаблID`. Функцию либо разобрать, либо оставить только `manifest`.

---

## Этап 6.2. `registeredIds` и обратимая `ссылка_1С`

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.2.1. Заменить `глТаблID` на экспортный словарь

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** нет колонки живой `ссылка` на все сущности выгрузки. `КУДиР_НайтиСсылку` / `КУДиР_НайтиИдПоСсылке` по полной таблице в горячих циклах удалены.

### 6.2.2. Регистрация только трёх типов

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** в `id_map.csv` нет строк `COUNTERPARTY` и `LEDGER`. Вызовы `КУДиР_ЗапомнитьID("COUNTERPARTY"…)` и `КУДиР_ЗапомнитьID("LEDGER"…)` удалены. `КонтрагентID` по-прежнему пишется во входные CSV как бизнес-поле.

### 6.2.3. Правило коллизии

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** ключ `entity_type + "|" + id`; нет ключа → записать строку CSV и словарь; ключ есть и `СистСсылка` совпадает → вторую строку не писать; ключ есть и строка другая → `FAILED`. Сравнение без `СокрЛП()`. Один объект выписки → несколько `PAYMENT` ID допустим.

### 6.2.4. Сериализация

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** `PAYMENT` → документ оплаты, `DOCUMENT` → счёт/накладная/услуги, `CONTRACT` → элемент справочника. Пустая обязательная `СистСсылка` — ошибка выгрузки, не подстановка представления.

### 6.2.5. Уникальность ledger без объекта

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** повторный `ledger_event_id` = `FAILED`. Словарь только строк ID, без `ЗначениеВСтрокуВнутр(Док)` на каждую корреспонденцию.

---

## Этап 6.3. Проход A и момент создания `run_id`

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.3.1. Только целевые клиенты

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt` — новая процедура на базе фильтра из `КУДиР_СобратьОплаты`
- **Готово, когда:** диапазон `ДатаНач…ДатаКон`, корреспонденции `50/51/55 → 62`, в RAM только множество `КонтрагентID` (+ ссылка контрагента для сальдо). Полных строк оплат в проходе A нет.

### 6.3.2. Пустая выборка

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** нет клиентов → пустая итоговая таблица, Python не вызывается, тяжёлый `run_id` можно не создавать.

### 6.3.3. Каталог запуска до ledger

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** при ненулевых клиентах сразу: `КУДиР_НовыйRunId`, `exchange/<run_id>/`, run-specific лог, `manifest.csv` с `schema_version=2`, открыт `id_map.csv`. Исчерпание `001…999` по-прежнему `FAILED`.

---

## Этап 6.4. Проход B: оплаты

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.4.1. История только выбранных клиентов

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** диапазон `ИсторияНач…ДатаКон`. Чужие исторические оплаты не добавляются. Цикл «накопить всех, потом удалить строки» удалён.

### 6.4.2. Агрегация до конца прохода

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** один `payment_id` не пишется при первом появлении. До конца B могут измениться `СуммаКоп` и справочные договор/вид расчётов (при расхождении — пустые, как сейчас). Dictionary `payment_id → строка`.

Семантика ID без изменений: банк = позиция + `НомерПроводки()`, ПКО = позиция, `НомерСтрокиВДокументе` пустой, `PaymentGranularity` выписки = `POSTING`.

### 6.4.3. Запись `payments.csv` и регистрация PAYMENT и CONTRACT

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** после конца B файл записан потоково; при первой регистрации каждого `payment_id` строка `id_map` ушла на диск; **каждый непустой договор каждой корреспонденции этой оплаты зарегистрирован как CONTRACT до очистки справочных полей** (5.9.4); множество `paymentIds` сохранено для ledger. Контроль коллизии ID выполняется до пропуска дубликата.

### 6.4.4. Очистка широкой таблицы оплат

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** после записи тексты назначений и прочие широкие колонки не живут до конца `КУДиР_Выполнить`. Для ledger достаточно `paymentIds`.

Поля `ДоговорID` / `ВидРасчетовID` в `payments.csv` остаются не-authoritative.

---

## Этап 6.5. Потоковый ledger

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.5.1. Писать и забывать

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** нет полной `таблLedger`. Цикл: подходящая проводка 62 → поля → `ledger_event_id` → проверка уникальности → строка CSV → забыть широкую строку.

Выборка `62,*;*,62;` и отсутствие дедупа по `document_id` сохраняются.

### 6.5.2. `payment_id` только у оплат из `payments.csv`

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** связка по `paymentIds`, не линейный обход таблицы оплат. Оплаты не фильтруются из ledger. Неплатёжный 62 выгружается с пустым `payment_id`.

### 6.5.3. Аналитика со стороны 62

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** контрагент/договор/вид расчётов по-прежнему с дебета или кредита, где 62. Формулы `глПослПозиция` / `глПослПроводка` / `глНомКорр` не сбрасывать внутри прохода.

### 6.5.4. Прогресс

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** в файл лога — ориентировочно каждые 500 проводок; `Состояние()` чаще; `ОбработкаПрерыванияПользователя()` сохранена. Сейчас шаг 20 слишком частый для файла.

---

## Этап 6.6. Потоковые документы и агрегаты НДС

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

Это ломающее изменение схемы. Без него Python не может считать фактический НДС в том же запуске.

### 6.6.1. Три вида, один обход, без полной таблицы

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** только `Счет`, `РасходнаяНакладная`, `ОказаниеУслуг`; интервал `ИсторияНач…MatchHorizonEnd`; дедуп Dictionary по `document_id`; клиент проверяется до тяжёлой работы; строка `documents.csv` пишется сразу; полной `таблДокументы` нет.

Абстрактный механизм неизвестных видов не проектировать. Закомментированный `СчетНаОплату` не включать.

### 6.6.2. Агрегаты колонки `НДС` (ТЗ №1 §15.1)

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** пока документ открыт: `ВыбратьСтроки` / `ПолучитьСтроку`; `НДС > 0` → облагаемая + НДС; иначе необлагаемая; строки не копятся в `ТаблицаЗначений`; отдельный `document_lines.csv` не создаётся; второго обхода «за НДС» нет; `СуммаКоп = Док.Итог("Сумма")`, не сумма только облагаемых.

Нет колонки `НДС` у вида → `FAILED` с именем вида. Пустая ТЧ при ненулевой сумме → три агрегата 0 и сообщение в лог, строка всё равно пишется. Расхождение `Облагаемая + Необлагаемая ≠ СуммаКоп` — лог, строка пишется. Формулу 5/105 вместо колонки не подставлять. Отрицательный `НДС` строки не класть в необлагаемые: это неподдерживаемый случай версии 1 — диагностика, агрегаты не утверждать как налоговый факт (ТЗ №1 §15.1).

Контроль коллизии `document_id` / `registeredIds` — **до** окончательного пропуска дубликата: ранний `documents.Exists → пропустить` не должен скрыть две разные системные ссылки на один ID.

Порядок колонок `documents.csv` — ТЗ №1 §15: три агрегата сразу после `СуммаКоп`.

### 6.6.3. Вид расчётов документа

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt` — `КУДиР_ДобавитьДокумент`, раскомментировать и довести `КУДиР_ВидРасчетовИзПроводок62`
- **Готово, когда:**
  - `Счет`: реквизит не читается, поля вида пустые, это не `FAILED`;
  - `RECEIVABLE`: только `ВидыРасчетовСПокупателями`, без fallback по другим именам;
  - пустой реквизит у проведённого → проводки 62; один вид → записать; несколько → пустые поля + диагностика, не `FAILED`; ни одного → `FAILED`;
  - непроведённый `RECEIVABLE` с пустым реквизитом допустим.

### 6.6.4. Регистрация DOCUMENT и CONTRACT

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** при первой записи документа — `DOCUMENT → ЗначениеВСтрокуВнутр(Док)` в `id_map`; договор документа — `CONTRACT`, если не пуст. Контрагент в `id_map` не пишется. Это **дополнение** к регистрации CONTRACT оплат из 5.9.4 / 6.4.3, а не единственный источник договоров.

### 6.6.5. Комментарий и отсутствие вида

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** нет реквизита `Комментарий` → пустая строка, не `FAILED`. Отсутствующий вид документа (`Документ.Счет` и т.п.) — сообщение, не пустая выборка без диагностики. Нет пустого `Попытка/Исключение`. Нет подстановки `СуммаКоп=0` / `Проведен=1`.

Способ проверить отсутствие реквизита — через метаданные конфигурации, не через глотание исключения. Helper вызывается для банковских документов, ПКО, документов ledger и трёх видов `documents.csv`. Таблица «только три вида КУДиР» не должна терять комментарий выписки/ПКО/прочего 62.

### 6.6.6. Прогресс документов

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** в файл лога ориентировочно каждые 200 документов.

---

## Этап 6.7. Сальдо и замена линейных поисков

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.7.1. `opening_balances.csv` по мере аналитики

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** расчёт через `БухгалтерскиеИтоги` на момент перед `ИсторияНач` использует **исправленные** параметры запроса из 5.9.1 (не режим 3 для договоров и видов расчётов). Оба остатка Дт/Кт, ключ аналитики, нулевые пары не пишутся, нет фиктивной `62.1`. Желательно писать строку CSV сразу, без полной `таблСальдо` до конца. Нет клиентов к этому моменту — уже обработано в 6.3.2; если клиенты есть, а сальдо считать не из чего — не подставлять нули «чтобы файл был».

### 6.7.2. Dictionary вместо переборов

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** удалены из горячих циклов `КУДиР_НайтиОплату`, `КУДиР_КлиентВВыборке`/`КУДиР_ЕстьИдВТаблице`, `КУДиР_НайтиСсылку` по полной таблице. Индексы — таблица «Структуры RAM» выше.

### 6.7.3. `КУДиР_КомментарийДок` не падает

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** критерий 4.6.9 плана исправлений сохранён: нет реквизита → `""`, нет `КУДиР_Сбой` внутри этой функции.

---

## Этап 6.8. Лог и разделение пиков EXPORT / LOAD

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-03 / проверено тестом исходника / не проверено в 1С

### 6.8.1. Убрать `глЛогТекст`

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** `КУДиР_ПеренестиЛогВКаталогЗапуска` не копирует накопленную строку. После создания `run_id` лог сразу пишется в `exchange/<run_id>/1c_export.log` (дубль в `exchange/1c_export.log` допустим, если не копится в RAM). DEBUG не на каждую проводку в файл.

### 6.8.2. Освобождение до Python

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt` — `КУДиР_Выполнить`
- **Готово, когда:** перед `КУДиР_ЗапуститьPython()` закрыты все TextStream выгрузки, обнулены/освобождены Dictionary и таблицы EXPORT, `registeredIds` уничтожен. `restore` ещё не существует.

### 6.8.3. Оборванный экспорт не зовёт Python

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** `глОшибка = 1` на любом этапе A–сальдо → Python не запускается, загрузчик не читает частичный комплект как успех.

---

## Этап 6.9. Загрузчик L1 / L2 / L3

**Статус:** реализовано в `kudir_export.txt` 2026.09.05-02/03 / проверено тестом исходника / не проверено в 1С

Сохраняются проверки этапа 5: `run_status`, допустимый статус, сверка `run_id`, все колонки `kudir_result.csv`, длина строк, `tax_ready=0` не блокирует matching, `FAILED` не загружается, частичная таблица по ошибке ID запрещена.

### 6.9.1. L1 — множества ID без широкой таблицы результата

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt` — переписать `КУДиР_ЗагрузитьИВосстановить`
- **Готово, когда:** первый проход по `kudir_result.csv` не складывает полные строки в промежуточную `ТаблицаЗначений`. Собираются только `PAYMENT|payment_id`, непустые `CONTRACT|contract_id`, непустые `DOCUMENT|matched_document_id`. Формат, обязательные поля, `run_id` и **все числовые поля как целые копейки** проверяются без объектов 1С (5.9.5). Инварианты ТЗ №1 §28. Дубль `result_row_id` — ошибка.

### 6.9.2. L2 — узкий `restore` из файла

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** один потоковый проход `id_map.csv` **логическими CSV-записями** (5.9.6); строки вне множеств L1 пропускаются; для попавших сохраняется `key → ссылка_1С` без `СокрЛП`. После файла: каждый обязательный ID L1 имеет ровно одну системную строку; отсутствие или коллизия → `FAILED`. Полный `id_map` в широкую таблицу не грузится.

### 6.9.3. L3 — `ЗначениеИзСтрокиВнутр`

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** второй проход `kudir_result.csv`; объект оплаты обязателен; договор и документ реализации — только если ID непустой и допустим ТЗ №1. Последовательность: найти системную строку → проверить непустоту → `ЗначениеИзСтрокиВнутр()` → проверить, что значение непустое **и нужного типа** → только затем `НомерДок` / `ДатаДок` / `Наименование`. Непустой результат неверного типа = ошибка восстановления. Fallback по номеру, дате, наименованию запрещён.

Пустой `contract_id` / `matched_document_id`, если допустим (`DEBT_UNRESOLVED`, незакрытый `ADVANCE`), не восстанавливается.

### 6.9.4. Ошибка восстановления

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`
- **Готово, когда:** можно собрать несколько ошибок ID/чисел/CSV за проход, но итоговая таблица полностью пустая. Нет чтения реквизитов после «НЕ восстановлен». Учитывается `глОшибка`. `COUNTERPARTY`/`LEDGER` загрузчик не ищет.

### 6.9.5. Тексты и `tax_ready`

- **Статус:** pending
- **Файлы:** `1cv77/kudir_export.txt`, `tests/test_kudir_loader_source.py`
- **Готово, когда:** убрано «цикл 2 не стартовать». Остаётся: matching загружается, книга не готова к сдаче. `SUCCESS_DEGRADED` — предупреждение, HIGH можно грузить. Суммы из копеек Python не пересчитываются.

---

## Этап 6.10. Приёмка

**Статус:** частично — 6.10.4 закрыт; 6.10.1–6.10.3 ждут живую 1С

Живой 1С 7.7 в CI нет. Контракт исходника — обязательно зелёный. Семантика фактов — контрольная и проблемная базы.

### 6.10.1. Контрольная база: сравнение с независимым эталоном

- **Статус:** pending
- **Готово, когда:** полный комплект CSV сверен с бухгалтерскими данными контрольной базы (ОСВ 62 по аналитике, выборка проводок). Со старой выгрузкой сравнивать только поля, которые A1–A3 и schema v2 **не** должны менять. Допустимые расхождения — список 5.9.7. Случайной выборки недостаточно.

`id_map.csv`: нет дублей ключа; только три типа; round-trip восстанавливает **исходный объект того же типа**, а не любое непустое значение. `ссылка_1С` со старым `СокрЛП(Объект)` не сравнивать.

### 6.10.2. Живой проблемный период

- **Статус:** pending
- **Готово, когда:** период, на котором текущая версия падает, даёт **хотя бы одну полную успешную цепочку** 1С v2 → Python → L1/L2/L3 на валидных данных. Отдельно: искусственные ошибки дают контролируемый `FAILED` и пустую таблицу. Сценарий «дойти до FAILED из‑за неготового Python v2» этап 6 не закрывает. Недостаточно «на маленькой базе не упало».

Проверить по логам/коду: нет полной `таблLedger`; `id_map` потоковый; ledger не регистрируется в `id_map`; структуры EXPORT освобождены до Python; `restore` только по ID из `kudir_result.csv`; повторный запуск даёт новый `run_id`; сальдо и копейки соответствуют 5.9.1–5.9.2.

### 6.10.3. Искусственно повреждённая `ссылка_1С`

- **Статус:** pending
- **Готово, когда:** битая/пустая системная строка → контролируемый `FAILED`, полностью пустая таблица, процесс жив.

### 6.10.4. Согласование с Python v2

- **Статус:** реализовано 2026.09.06 — `csv_io.SCHEMA_VERSION=2`, три агрегата в `DOCUMENTS_FIELDS`, golden v2, валидация в `loaders.py`; автоконвертации нет. Расчёт НДС (`tax.py`, B1–B3) — отдельно, не закрывает этот чекпоинт
- **Файлы вне 1С (блокеры прогона книги):** `src/kudir_proto/csv_io.py`, golden, `src/kudir/loaders.py`, `src/kudir/tax.py`
- **Готово, когда:** Python принимает `schema_version=2` и три агрегата; автоконвертации v1→v2 нет. Этот чекпоинт не закрывает этап 6 сам по себе, но без него живой прогон КУДиР нельзя считать налоговым.

Если Python ещё v1, 1С всё равно пишет v2: лучше явный `FAILED` Python, чем молчаливая схема v1 без НДС.

---

## Порядок внедрения внутри патча

Один конечный патч потока, но **после** закрытия 5.9. Внутри файла — такая последовательность, чтобы промежуточные состояния компилировались:

0. Факты 5.9.1–5.9.4 (сальдо, копейки, ПКО, CONTRACT) — можно ещё на старых таблицах, это намеренно.
1. Хелперы CSV-потока, логическая CSV-запись и Dictionary (6.1, 5.9.6, каркас 6.7.2).
2. `registeredIds` + запись `id_map` (6.2) — системная строка и три типа.
3. Переставить `КУДиР_Выполнить`: A → run_id → B (с CONTRACT) → stream ledger → stream documents+НДС → сальдо 5.9.1 (6.3–6.7).
4. Лог в файл, освобождение до Python (6.8).
5. Загрузчик L1/L2/L3 с A5/A6 (6.9).
6. Тесты исходника зелёные (6.0), включая запреты 5.9.
7. Приёмка 6.10 с независимым эталоном (5.9.7).

Не делать «сначала P0 на старых таблицах, потом P1»: полный `таблLedger` + потоковая запись всё равно держит пик RAM. Не делать поток, пока сальдо в режиме 3 и копейки ломают сторно.

P2 (нарезка операций по месяцам) — только если после 6.10.2 объект `Операция` на диапазоне `ИсторияНач…ДатаКон` / `MatchHorizonEnd` всё ещё валит процесс. Тогда отдельный мини-план: куски без пересечений, заголовок один раз, `глПосл*` между кусками ledger не сбрасывать.

---

## Чеклист готовности `kudir_export.txt`

Перенос критериев ТЗ №1 §29 и ТЗ №4 §15, которые должен закрыть этот файл:

1. Запрещённый старый matching/FIFO/обрезка номера в модуль не перенесены.
2. `manifest.csv`: `run_id`, `quarter_end`, `match_horizon_end`, `schema_version=2`.
3. `payments.csv`: история, `is_target`, `НомерПроводкиВДокументе`; договор/вид не authoritative.
4. Банк: позиция + `НомерПроводки()`; ПКО: позиция; документ: `ПолучитьПозицию()`.
5. `ledger62.csv`: все 62, `ledger_event_id`, `source_document_id`, `payment_id` у оплат, без дедупа, без хранения всей таблицы.
6. `documents.csv`: роль, вид расчётов по правилам конфигурации, агрегаты НДС, горизонт не обрезан кварталом `ДатаКон`.
7. `opening_balances.csv` обязателен, ключ аналитики, оба остатка, разворот субконто режимом 1, субсчета 62.
8. `Комментарий` и `ПервичныйДокумент` выгружаются; нет реквизита комментария → пусто; helper для всех видов, где он вызывается.
9. `id_map.csv`: только три типа; CONTRACT оплат и документов; `ссылка_1С = ЗначениеВСтрокуВнутр()`; коллизия ID = `FAILED`.
10. Табличные строки товаров не в RAM и не в CSV; второго прохода документов нет.
11. 1С не считает НДС/УСН и не решает DEBT/ADVANCE.
12. После экспорта RAM EXPORT освобождена; Python; L1/L2/L3; `ЗначениеИзСтрокиВнутр()` нужного типа; ошибка числа/CSV/ID → пустая таблица; `tax_ready=0` не блокирует.
13. CSV-протокол ТЗ №1 §30, включая логическую запись для `id_map`.
14. Копейки сохраняют знак сторно. ПКО — одна CASH-оплата.

---

## Вне скоупа этого файла

| Работа | Почему отдельно |
| --- | --- |
| `src/kudir/tax.py` | ТЗ №2; читает агрегаты, которые отдаст этот модуль |
| Golden v2 и `SCHEMA_VERSION = "2"` в Python | Закрыто 06.09.2026 (чекпоинт 6.10.4); `tax.py` / B1–B3 — отдельно |
| Живой HTTP-parser | Уже этап 4.5; 1С только запускает `run_kudir.bat` |
| Semantic reranker, возвраты, эквайринг | Версия 1 не требует |
| Нарезка по группам клиентов (P3) | Отдельное мини-ТЗ после замера P2 |

---

## Следующий конкретный шаг

Живая приёмка **6.10.1–6.10.3**: контрольная база (ОСВ 62, допустимые расхождения 5.9.7), проблемный период до полного цикла, битая `ссылка_1С`. Контракт Python schema v2 (**6.10.4**) закрыт; налог по агрегатам (`tax.py`, аудит B1–B3) — соседняя работа после живой сверки или параллельно.
