# ТЗ №1. 1С 7.7

## Подготовка исходных данных для Python-модуля анализа доходов КУДиР и загрузка результата

### 1. Назначение

Разработать модуль 1С 7.7, который выполняет только получение достоверных исходных данных из информационной базы и обмен с Python.

1С НЕ должна выполнять сложное сопоставление оплат с документами.

Общая схема:

```text
1С 7.7
   ↓
payments.csv
ledger62.csv
documents.csv
   ↓
Python
   ↓
kudir_result.csv
   ↓
1С 7.7
   ↓
СформироватьТаблицуДоходовЧистовая()
```

---

# 2. Важное требование: переиспользовать существующий код

При разработке запрещается без необходимости переписывать уже существующий и проверенный код из `доходы-2.txt` и `Анализ-взаирасчетов.txt`.

Перед созданием новой функции Cursor должен проверить, нет ли уже работающей функции с такой функциональностью.

В частности, необходимо максимально переиспользовать:

```text
ПолучитьНомерСчетаИзКомментария()
ПолучитьСтрокуПервичногоДокумента()
ПолучитьНомерИДатуРСдляУПДИзСтрокиПервичногоДокумента()
```

а также существующие механизмы обхода:

```text
Операция
БухгалтерскиеИтоги
ПолучитьПозицию()
СформироватьПозициюДокумента()
```

Существующий механизм:

```text
РазобратьНазначениеПлатежа()
```

не удалять.

Однако в новом основном процессе сложный разбор желательно выполнять на стороне Python, используя уже существующий Python-сервис/модуль, который сейчас вызывается из 1С.

Не писать второй независимый классификатор с нуля.

---

# 3. Граница ответственности

## 1С делает

* получает платежи;
* получает проводки по 62;
* получает контрагентов;
* получает договоры;
* получает виды расчётов;
* получает исходные тексты;
* получает комментарии документов;
* получает счета на оплату;
* получает документы отгрузки;
* получает документы оказания услуг;
* получает позиции документов;
* формирует CSV;
* запускает/вызывает Python;
* загружает результат;
* формирует итоговую `ТаблицаЗначений`.

## 1С не делает

* FIFO/LIFO;
* поиск комбинаций сумм;
* определение документа, который оплачивается;
* эвристический matching;
* ML/LLM;
* определение score;
* принятие решения «аванс или долг» по тексту.

---

# 4. Периоды

Параметры:

```text
ДатаНач
ДатаКон
МесяцевИстории = 3
```

Целевые платежи КУДиР выбираются:

```text
ДатаНач ... ДатаКон
```

Но история взаиморасчётов и документы выгружаются начиная не с `ДатаНач`.

Начало исторического периода:

```text
1 января года ДатаНач
минус МесяцевИстории
```

По умолчанию:

```text
МесяцевИстории = 3
```

Например для КУДиР 2026 года:

```text
ДатаНач = 01.01.2026

История начинается:
01.10.2025
```

То есть дополнительно захватываются:

```text
октябрь 2025
ноябрь 2025
декабрь 2025
```

Параметр должен быть изменяемым.

---

# 5. payments.csv

Файл содержит платежи, которые необходимо включить в анализ КУДиР.

Основные корреспонденции:

```text
50 / 62
51 / 62
55 / 62
```

В том числе:

* поступление на расчётный счёт;
* наличная оплата;
* иные используемые денежные счета.

---

# 6. Выписка и ПКО обрабатываются по-разному

## Банковская выписка

Один документ `Выписка` может содержать несколько самостоятельных платежей.

Каждая строка оплаты должна стать отдельным `payment_id`.

Необходимо сохранять:

```text
НомерСтрокиВДокументе
```

или другой порядковый номер строки.

## Приходный кассовый ордер

Для ПКО весь документ является одной оплатой.

У него нет необходимости искать строку платежа внутри документа.

Для него:

```text
НомерСтрокиВДокументе = 0
```

или пустое значение.

Уникальность обеспечивается самим документом.

---

# 7. payment_id

Каждая самостоятельная оплата получает уникальный технический идентификатор.

Например:

```text
BANK|Выписка|0296|2025-12-30|позиция|2
```

или:

```text
CASH|ПКО|00125|2026-06-30|позиция
```

Формат не принципиален.

Требование:

одна и та же операция должна иметь одинаковый `payment_id/event_id` во всех CSV.

---

# 8. Поля payments.csv

Минимально:

```text
payment_id
event_id

PaymentKind
PaymentGranularity

ДатаОперации
ПозицияДокумента
НомерСтрокиВДокументе

ВидДокументаОплаты
НомерДокументаОплаты
ДатаДокументаОплаты

СуммаКоп

СчетДебета
СчетКредита

КонтрагентID
Контрагент

ДоговорID
Договор
НомерДоговора
ДатаДоговора

ВидРасчетовСПокупателем

СодержаниеПроводки
Содержание
ПредставлениеПроводки

НазначениеПлатежа
ПервичныйДокумент

КомментарийДокументаОплаты
```

`PaymentKind`:

```text
BANK
CASH
OTHER
```

`PaymentGranularity`:

```text
ROW
DOCUMENT
```

---

# 9. Обязательно выгружать оригинальные тексты

Не нормализовывать смысл текста внутри 1С.

Python должен получить исходные:

```text
СодержаниеПроводки
Содержание
ПредставлениеПроводки
НазначениеПлатежа
ПервичныйДокумент
Комментарий
```

Именно в этих полях часто находится прямое указание:

```text
Оплата по счету ...
Оплата по акту ...
Оплата по накладной ...
```

---

# 10. Комментарий документа

Для всех документов, где существует реквизит `Комментарий`, необходимо выгружать его полностью.

Это новое обязательное поле.

Особенно важно для:

```text
Отгрузка товаров
Оказание услуг
Счет на оплату
Приходный кассовый ордер
```

Примеры стандартизированного текста:

```text
Введен на основании: Счет на оплату № 00002 от 06 Января 2026 г.
```

или:

```text
Введен на основании: Отгрузка товаров № Н00570 от 30 Июня 2026 г.
```

Такие строки считаются очень ценными, поскольку они формируются программой, а не пользователем.

1С не должна сама анализировать их смысл.

Она должна передать текст Python без изменений.

---

# 11. Аналитика счета 62

Контрагент, договор и вид расчётов берутся именно с той стороны проводки, где находится 62.

Например:

```text
Дт 62.1 Кт 90.1
```

брать:

```text
Опер.Дебет.Контрагенты
Опер.Дебет.Договоры
Опер.Дебет.ВидыРасчетовСПокупателями
```

Для:

```text
Дт 51 Кт 62.1
```

брать:

```text
Опер.Кредит.Контрагенты
Опер.Кредит.Договоры
Опер.Кредит.ВидыРасчетовСПокупателями
```

---

# 12. ledger62.csv

Содержит историю движений по 62:

```text
01.10 предыдущего года ... ДатаКон
```

для клиентов, имеющих платежи анализируемого периода.

Одна строка = одна бухгалтерская проводка.

Поля:

```text
event_id

ДатаОперации
ПозицияДокумента
НомерПроводкиВДокументе

ВидДокумента
НомерДокумента
ДатаДокумента

СуммаКоп

СчетДебета
СчетКредита
Счет62
Сторона62

КонтрагентID
Контрагент

ДоговорID
Договор
НомерДоговора
ДатаДоговора

ВидРасчетовСПокупателем

СодержаниеПроводки
Содержание
ПредставлениеПроводки

КомментарийДокумента
ПервичныйДокумент
```

`Сторона62`:

```text
D
K
```

---

# 13. documents.csv

Необходимо дополнительно выгружать справочный набор документов.

Причина:

не все необходимые документы отражаются непосредственно в проводках 62.

В частности:

```text
Счет на оплату
```

обычно нужен для установления связи:

```text
Платеж
→ Счет
→ Отгрузка / Оказание услуг
```

---

# 14. Какие документы выгружать

Минимально:

```text
Счет на оплату
Отгрузка товаров
Оказание услуг
```

а также иные документы реализации, которые фактически встречаются в базе и создают задолженность.

Для ПКО сам ПКО уже находится в `payments.csv`, но его комментарий также должен быть выгружен.

---

# 15. Поля documents.csv

```text
document_id

ВидДокумента
НомерДокумента
ДатаДокумента
ПозицияДокумента

КонтрагентID
Контрагент

ДоговорID
Договор
НомерДоговора

СуммаКоп

Комментарий

Содержание
```

Если доступно:

```text
СуммаБезНДС
СуммаНДС
```

можно выгрузить на будущее, но текущий алгоритм НДС их пока не использует.

---

# 16. Все счета на оплату

В `documents.csv` должны попасть все счета на оплату в историческом периоде для анализируемых клиентов.

Даже если счёт:

* не формирует проводки;
* полностью оплачен;
* сам по себе не является документом задолженности.

Счёт нужен как промежуточная сущность для построения связи:

```text
Оплата
↓
Счет №00002
↓
Комментарий документа реализации:
"Введен на основании: Счет на оплату №00002 ..."
↓
Отгрузка товаров / Оказание услуг
```

---

# 17. Отгрузки и услуги

Для каждого документа отгрузки или оказания услуг обязательно выгрузить:

```text
номер
дату
сумму
контрагента
договор
комментарий
позицию документа
```

Комментарий особенно важен.

---

# 18. ПКО

Для ПКО обязательно выгружать:

```text
КомментарийДокументаОплаты
```

Если там имеется:

```text
Введен на основании: Отгрузка товаров № Н00570 от 30 Июня 2026 г.
```

Python сможет практически напрямую установить оплачиваемый документ.

---

# 19. Существующий разбор платежных строк

Не удалять и не переписывать существующий работающий код извлечения:

```text
номера счета
первичного документа
номера и даты документа
```

Если в новом процессе соответствующая обработка переносится в Python, существующий алгоритм должен использоваться как исходная проверенная реализация.

Перед переписыванием в Python необходимо создать тесты:

```text
исходная строка
→ результат старой функции
```

и новый модуль должен выдавать тот же результат минимум на существующей тестовой выборке.

---

# 20. Что 1С НЕ должна решать

Даже если 1С обнаружила:

```text
Оплата по счету №00002
```

она не должна сама искать отгрузку.

Она передаёт Python:

```text
номер счета
исходный текст
список счетов
список отгрузок
комментарии
```

Именно Python строит цепочку.

---

# 21. Суммы

В CSV:

```text
СуммаКоп
```

целое число.

Например:

```text
5500 руб. = 550000
```

Не использовать `float`.

---

# 22. Порядок событий

Для Python необходимо обеспечить возможность сортировки:

```text
ДатаОперации
ПозицияДокумента
НомерПроводкиВДокументе
```

Для ПКО последний компонент равен `0`.

---

# 23. Экспортируемая история

История ограничена.

По умолчанию:

```text
ИсторияНач =
01 января года анализируемого периода - 3 месяца
```

Например:

```text
КУДиР 2026
→ история с 01.10.2025
```

Нет необходимости выгружать всю историю клиента с момента создания базы.

---

# 24. Результат Python

Python возвращает:

```text
kudir_result.csv
```

После загрузки 1С формирует существующую структуру:

```text
ДокументОплаты
НомерДокументаОплаты
ДатаПлатежа
СуммаПлатежа

Договор
НомерДоговора

СодержаниеЗаписи

СуммаВАванс
СуммаВОпалату

СуммаСНДС
СуммаНДС

СуммаВДоход
```

---

# 25. Разбиение платежа

Одна исходная оплата может породить несколько строк.

Например:

```text
10 000 поступило
7 000 погашает задолженность
3 000 является авансом
```

Результат:

```text
строка 1:
СуммаПлатежа = 7000
СуммаВОпалату = 7000

строка 2:
СуммаПлатежа = 3000
СуммаВАванс = 3000
```

---

# 26. СуммаВДоход

На текущем этапе:

```text
СуммаВДоход = СуммаПлатежа
```

То есть либо:

```text
СуммаВДоход = СуммаВАванс
```

либо:

```text
СуммаВДоход = СуммаВОпалату
```

Одновременно `СуммаВАванс` и `СуммаВОпалату` ненулевыми быть не могут.

---

# 27. НДС

Пока:

```text
СуммаСНДС = 0
СуммаНДС = 0
```

НДС в данной задаче не анализируется.

---

# 28. Обязательные контрольные проверки

Для каждого `payment_id`:

```text
сумма всех результирующих частей
=
исходная сумма платежа
```

И для каждой строки:

```text
СуммаВАванс + СуммаВОпалату
=
СуммаПлатежа
```

---

# 29. Критерии готовности части 1С

1. Не переписан заново работающий существующий код без необходимости.
2. Формируется `payments.csv`.
3. Поддерживаются выписки.
4. Поддерживаются ПКО.
5. Выписка делится по строкам.
6. ПКО рассматривается как одна оплата.
7. Формируется `ledger62.csv`.
8. Формируется `documents.csv`.
9. Выгружаются все счета анализируемых клиентов.
10. Выгружаются отгрузки и оказания услуг.
11. Выгружается `Комментарий`.
12. Выгружается `ПервичныйДокумент`.
13. Аналитика берётся со стороны счета 62.
14. История начинается по умолчанию с 1 октября предыдущего года.
15. 1С не выполняет эвристический matching.
16. Загружается `kudir_result.csv`.
17. Формируется существующая `СформироватьТаблицуДоходовЧистовая()`.
