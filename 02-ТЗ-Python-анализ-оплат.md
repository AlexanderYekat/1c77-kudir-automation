# ТЗ №2. Python

## Анализ платежей, поиск оплачиваемых документов и формирование строк КУДиР

### 1. Назначение

Python-программа является основным аналитическим модулем версии 1: matching оплат, а не готовая налоговая КУДиР.

Оплата не определяет сумму НДС. Версия 1 раскладывает поступление на погашение долга и аванс и ищет документы — в том числе реализации **после оплаты, но до конца того же квартала**. НДС считает второй цикл, который в этом ТЗ не реализуется.

Вход:

```text
manifest.csv
payments.csv
ledger62.csv
documents.csv
opening_balances.csv
```

`opening_balances.csv` обязателен. Отсутствие файла — ошибка, расчёт с нуля запрещён.

Выход:

```text
kudir_result.csv
matches.csv
unresolved.csv
analysis.log
run_status.csv
```

В `kudir_result.csv` попадают только оплаты с `is_target=1`. Исторические оплаты участвуют в matching и восстанавливают остатки документов.

Через CSV передаются ID и скаляры, не объекты 1С.

---

# 2. Ключевое требование: не писать существующий разбор заново

Parser является внешней зависимостью и в этот репозиторий не входит. Новый parser не разрабатывается и не копируется.

Он уже умеет определять:

```text
смысл платежа
тип упомянутого документа
номер документа
дату документа
номер счета
```

Приоритет:

```text
1. вызывать существующий HTTP-сервис http://127.0.0.1:8765/parse1c;
2. инкапсулировать вызов в legacy_parser_adapter.py;
3. покрыть regression-тестами реальные строки;
4. только после этого добавлять новые правила matcher.
```

Если parser недоступен: для этой оплаты текстовый разбор пропускается, matching продолжается по суммам и основаниям. Система не падает целиком. Итог запуска:

```text
SUCCESS           — parser был доступен
SUCCESS_DEGRADED  — parser недоступен или часть строк не разобрана
FAILED            — ошибка данных, схемы или инварианта
```

`SUCCESS` и `SUCCESS_DEGRADED` — код завершения 0. `FAILED` — ненулевой код, `kudir_result.csv` не создаётся. Статус пишется в `run_status.csv`. 1С при `SUCCESS_DEGRADED` показывает предупреждение; строки `HIGH`, полученные без parser, загружать можно.

Перед разбором проверять `GET /health` (если сервис его отдаёт). Одинаковые строки кэшировать на время запуска. Regression: unit-тесты на сохранённых ответах parser; отдельный integration-тест живого сервиса, который можно пропускать, если сервис не запущен.

Формат запроса/ответа — как у текущего клиента в `доходы-2.txt`. Адрес задаётся конфигурацией, по умолчанию `http://127.0.0.1:8765/parse1c`.

Не создавать новый независимый parser с нуля.

---

# 3. Архитектура Python

Рекомендуемая структура:

```text
src/
    loaders.py
    legacy_parser_adapter.py
    document_reference_parser.py
    base_document_links.py
    ledger.py
    matcher.py
    scoring.py
    combinations.py
    semantic.py
    kudir.py
    diagnostics.py
config/
    scoring.yaml
```

Особенно важно выделить:

```text
legacy_parser_adapter.py
```

который инкапсулирует уже существующий рабочий инструмент.

---

# 4. Этапы обработки платежа

Оплаты обрабатываются в хронологическом порядке:

```text
ДатаОперации
ПозицияДокумента
НомерПроводкиВДокументе
```

Ключ одинаков для `payments.csv` и `ledger62.csv`. `НомерСтрокиВДокументе` пуст и в порядок не входит. Исторические оплаты обрабатываются вместе с целевыми; в результат попадают только `is_target=1`.

Для всей области одного контрагента matcher идёт этапами блокировки, а не одним greedy-проходом:

```text
1. восстановить состояние взаиморасчётов на момент оплаты;
2. определить сумму погашения долга и аванса;
3. заблокировать уникальные машинные ссылки
   (ПКО / «Введен на основании»);
4. зафиксировать уникальные прямые ссылки и допустимые частичные оплаты;
5. зафиксировать уникальные совпадения через счёт
   по документам, существовавшим на момент оплаты;
6. зафиксировать уникальные совпадения остатка;
7. решить оставшиеся конфликты ограниченным поиском
   внутри одного контрагента;
8. всё ниже порога — DEBT_UNRESOLVED;
9. semantic reranker в версии 1 не обязателен;
10. после прохода по всем оплатам — закрыть несвязанный ADVANCE
    реализациями того же квартала, в том числе после даты оплаты.
```

FIFO и скрытый greedy как основной алгоритм запрещены.

Этапы 1–9 не привязывают аванс к будущей реализации. Это делает только этап 10.

---

# 5. Разделение на долг и аванс

Это чистая бухгалтерская арифметика.

Пример:

```text
долг перед платежом = 7 000
платеж = 10 000
```

Результат:

```text
debt_part = 7 000
advance_part = 3 000
```

Никакая LLM этого не определяет.

На этом шаге НДС ещё не считается. `debt_part` потом ищет прошлую реализацию, `advance_part` после хронологического прохода ищет реализацию того же квартала.

---

# 6. Первый и самый сильный путь: прямое указание документа

Сначала существующим parser'ом анализируются:

```text
СодержаниеПроводки
Содержание
ПредставлениеПроводки
НазначениеПлатежа
ПервичныйДокумент
КомментарийДокументаОплаты
```

Parser может обнаружить, например:

```text
НАКЛАДНАЯ № Н00570 от 30.06.2026
```

или:

```text
АКТ №25 от 10.08.2026
```

---

# 7. Прямой документ + сумма в пределах остатка

Если найдена уникальная ссылка на конкретный документ задолженности того же контрагента и документ существовал на момент оплаты:

```text
0 < сумма_к_распределению <= текущий непогашенный остаток документа
```

связь фиксируется сразу.

- полное закрытие остатка: `DIRECT_DOCUMENT_EXACT`, `HIGH`;
- частичная оплата: `HIGH_PARTIAL_DIRECT`, `HIGH`.

Стандартный общий matching в этом случае не нужен.

Частичная оплата — нормальный случай, а не ошибка суммы.

---

# 8. Прямой документ, платёж больше остатка

Если ссылка уникальна, но сумма больше остатка указанного документа:

```text
остаток → этот документ (DIRECT_DOCUMENT_EXACT / HIGH_PARTIAL_DIRECT)
хвост  → дальнейший matching или ADVANCE
```

Если ссылка неоднозначна (несколько документов) или сумма не помогает выбрать кандидата — не фиксировать автоматически. Документ получает бонус `DIRECT_DOCUMENT_HINT` (например `+100`) и идёт в общий цикл.

Точная сумма сама по себе сильна только при единственном кандидате. Два документа по 10 000 и платёж 10 000 однозначной связи не создают.

---

# 9. Второй основной путь: оплата по счету

На практике значительная доля платежей содержит именно:

```text
Оплата по счету ...
```

При этом документ задолженности — не счет, а:

```text
Отгрузка товаров
```

или:

```text
Оказание услуг
```

Поэтому необходимо построить цепочку:

```text
Платеж
→ Счет на оплату
→ документ, введенный на основании счета
→ задолженность или закрытие аванса
```

Реализация по счёту может быть раньше оплаты (погашение долга) или позже оплаты в том же квартале (закрытие аванса). Оба случая обязательны.

Счёт в версии 1 — только связка. Состав НДС по строкам товаров счёта — цикл 2, не этот модуль.

---

# 10. Поиск счета

Использовать существующий рабочий parser.

Например:

```text
Оплата по счету №00002
```

даёт:

```text
Тип = СЧЕТ
Номер = 00002
```

Если известна дата:

```text
Дата = ...
```

также использовать её.

Затем найти счет в `documents.csv` по:

```text
Контрагент
Номер
Дата — если известна
```

---

# 11. Связь Счет → Отгрузка / Оказание услуг

После нахождения счета найти документы:

```text
Отгрузка товаров
Оказание услуг
```

в комментарии которых имеется стандартизированная ссылка:

```text
Введен на основании: Счет на оплату № 00002 от 06 Января 2026 г.
```

Так как комментарий формируется программой, эта связь считается очень сильной.

Внутренний тип связи:

```text
BASE_DOCUMENT_LINK
```

Для погашения долга связанная реализация должна существовать на момент оплаты. Для этапа 10 та же цепочка через счёт ищется среди реализаций после оплаты до конца квартала.

---

# 12. Нормализация ссылки «Введен на основании»

Не сравнивать строку целиком посимвольно.

Разобрать её в структуру:

```text
base_type
base_number
base_date
```

Например:

```text
base_type = INVOICE
base_number = 00002
base_date = 2026-01-06
```

Поддержать лишние пробелы и различные регистры.

---

# 13. Счет → один документ → сумма в пределах остатка

Если:

```text
оплата → счет №00002
```

и найден ровно один связанный документ задолженности, и:

```text
0 < сумма_к_распределению <= остаток документа
```

то соответствие фиксируется сразу (`INVOICE_BASE_EXACT` или частичный аналог, `HIGH`).

---

# 14. Счет → несколько документов

Один счет теоретически может стать основанием нескольких документов.

Тогда:

```text
не выбирать первый найденный.
```

Проверить:

1. точное совпадение одной суммы;
2. точное совпадение с остатком;
3. сумму нескольких связанных документов;
4. другие признаки.

Если однозначности нет — перейти к стандартному matching.

Но документы, связанные с найденным счетом, получают очень высокий score.

---

# 15. Счет найден, платёж больше остатка связанного документа

Остаток относится на связанный документ, хвост идёт в matching или аванс.

Если связанных документов несколько и однозначности нет — не привязывать насильно. Связь через счет остаётся сильной подсказкой.

---

# 16. ПКО: отдельный быстрый путь

Для `PaymentKind=CASH` прежде всего анализировать:

```text
КомментарийДокументаОплаты
```

Частый вариант:

```text
Введен на основании:
Отгрузка товаров № Н00570 от 30 Июня 2026 г.
```

Это практически прямая машинная ссылка на документ.

---

# 17. ПКО + сумма в пределах остатка

Если из комментария ПКО найден уникальный документ и:

```text
0 < сумма ПКО (или её долговой части) <= остаток документа
```

фиксировать сразу:

```text
CASH_BASE_EXACT или HIGH_PARTIAL_DIRECT
HIGH
```

---

# 18. ПКО + сумма больше остатка

Остаток — на указанный документ, хвост — дальше. Если ссылка неоднозначна — бонус кандидата и общий matcher.

---

# 19. Стандартный matching

Он запускается только после прямых путей.

Кандидаты погашения долга должны соответствовать:

```text
тот же контрагент
документ существовал на момент оплаты
имеется непогашенный остаток
```

Кандидаты закрытия аванса задаются разделом 21.1, а не этим списком.

---

# 20. Другой контрагент — жёсткий запрет

```text
Контрагент оплаты != Контрагент документа
```

→ кандидат запрещён.

Это hard constraint.

---

# 21. Будущий документ — запрет только для долга

Для `DEBT` обычный платеж не может оплачивать документ, который ещё не существовал.

Использовать:

```text
Дата
ПозицияДокумента
```

Это hard constraint для погашения задолженности.

Ранее полученный аванс — отдельная ситуация: его как раз нужно связать с реализацией после оплаты, если она в том же квартале.

---

# 21.1. Закрытие аванса реализациями того же квартала

После того как все оплаты прошли этапы 1–9, для каждой несвязанной части `ADVANCE` искать документы `RECEIVABLE` того же контрагента:

```text
документ позже оплаты (Дата / ПозицияДокумента)
дата документа <= КонецКвартала(дата оплаты)
документ проведён и не помечен на удаление
ещё не полностью закрыт привязками DEBT этого запуска
```

Квартал берётся **от даты этой оплаты**, не от `ДатаКон`. Аванс 10 января закрывается реализациями по 31 марта включительно, даже если отчётный период обрезан 31 января — при условии, что такие документы выгружены.

Остаток взаиморасчётов по реализации **не** является условием этапа 10. Отгрузка в счёт аванса часто сразу закрывается зачётом 62, и дебетовый остаток по ней уже нулевой. Лимит — сумма документа минус суммы, уже связанные как `DEBT` / `DEBT_UNRESOLVED` с этой реализацией.

Приоритет связи, тот же дух scoring, что и для долга:

```text
1. цепочка оплата → счёт → реализация «Введен на основании»
2. уникальная прямая ссылка в назначении на эту будущую реализацию
3. точный остаток / полная сумма / комбинация сумм
4. договор, вид расчётов, товар/услуга
5. обратная хронология внутри окна квартала:
   ближайшая реализация после оплаты
```

Типы связи:

```text
ADVANCE_INVOICE_BASE        — через счёт
ADVANCE_SAME_QUARTER_DIRECT — прямая ссылка на будущую реализацию квартала
ADVANCE_SAME_QUARTER_AMOUNT — по сумме / остатку в окне квартала
```

`row_type` остаётся `ADVANCE`. `matched_document_id` заполняется найденной реализацией. `match_type` — один из типов выше. `confidence` и пороги — те же правила `scoring.yaml`.

Частичное закрытие:

```text
аванс 105 000
до конца квартала отгружено 63 000
```

даёт две строки:

```text
ADVANCE 63 000  — с matched_document_id реализации
ADVANCE 42 000  — без документа (незакрытый остаток)
```

Несколько реализаций в счёт одного аванса — несколько строк `ADVANCE` с разными документами. Сумма частей равна исходному авансу.

Реализация следующего квартала в версии 1 не связывается с этим авансом: остаток так и остаётся `ADVANCE` без документа.

Этап 10 не пересматривает уже зафиксированные `DEBT` / `DEBT_UNRESOLVED` и не превращает аванс в долг. Бухгалтерское разделение на дату оплаты не меняется: закрытая отгрузкой сумма всё равно `ADVANCE`, просто с документом. Документ нужен циклу 2, чтобы взять фактический НДС из реализации.

Если одному авансу подходят несколько реализаций и однозначности нет — не выбирать первую попавшуюся. Ниже порога оставить `ADVANCE` без документа и записать случай в `unresolved.csv`.

---

# 21.2. Чего этап 10 не делает

Не считает НДС. Не читает табличную часть счёта. Не применяет 5/105. Не смотрит реализации следующего квартала.

Цикл 2, отдельно:

```text
DEBT с реализацией
  → НДС из реализации

ADVANCE с реализацией того же квартала
  → НДС из этой реализации

ADVANCE без документа, но есть счёт
  → состав НДС/без НДС по строкам счёта

ADVANCE без документа и без счёта
  → расчётный НДС 5/105 с остатка
```

Это не требования к коду версии 1. В версии 1 `vat_kopecks = 0`.

---

# 22. Договор НЕ является hard constraint

Это важное изменение.

Несовпадение договора:

```text
НЕ должно исключать документ.
```

Совпадающий договор:

```text
сильный плюс.
```

Разный договор:

```text
штраф,
но не запрет.
```

Например:

```text
same contract       +40
different contract  -20
unknown contract      0
```

Конкретные веса вынести в настройки.

---

# 23. Почему разный договор допускается

В реальной базе возможны:

* неверно выбранный договор в платежной строке;
* разные технические договоры;
* исторические изменения;
* ошибки пользователя;
* отличия договора оплаты от договора документа.

Поэтому если имеются сильные признаки:

```text
прямая ссылка на счет
+
ссылка "Введен на основании"
+
точная сумма
```

они имеют право перебить несовпадение договора.

При этом в `reason` необходимо отметить:

```text
CROSS_CONTRACT_MATCH
```

---

# 24. Какой договор возвращать

Если платеж сопоставлен с конкретным документом задолженности:

```text
Договор в строке DEBT
=
договор найденного документа задолженности
```

Не надо искусственно считать разные договоры одинаковыми.

Для `ADVANCE` с найденной реализацией того же квартала:

```text
Договор
=
договор этой реализации
```

Для `ADVANCE` без документа:

```text
Договор
=
договор платежа
```

---

# 25. Приоритеты стандартного matching

Рекомендуемый порядок для погашения долга:

```text
1. прямой документ + точная сумма
2. счет → документ + точная сумма
3. ПКО → основание + точная сумма
4. точное совпадение остатка документа
5. точное совпадение полной суммы документа
6. несколько оплат = документ
7. одна оплата = несколько документов
8. закрытие остатка после предыдущей частичной оплаты
9. связь через счет/основание без точной суммы
10. договор
11. вид расчетов
12. товар/услуга
13. текстовая семантика
14. обратная хронология
```

Закрытие аванса (этап 10) выполняется отдельно после этого списка и не вставляет будущую реализацию в погашение долга.

---

# 26. Точные совпадения суммы

Сильными считать оба случая:

```text
платеж = первоначальная сумма документа
```

и:

```text
платеж = текущий остаток документа
```

Второй случай даже важнее для частично оплаченного документа.

---

# 27. Несколько оплат → один документ

Пример:

```text
Документ = 5 500

Оплата 1 = 4 500
Оплата 2 = 1 000
```

Получить:

```text
4 500 + 1 000 → 5 500
```

---

# 28. Одна оплата → несколько документов

Пример:

```text
Документы:
4 000
6 000

Платеж:
10 000
```

Получить две связи.

---

# 29. Очень важный случай закрытия остатка

Если:

```text
документ = 5 500
```

уже подобрана оплата:

```text
4 500
```

и имеется:

```text
1 000
```

то:

```text
1 000 = остаток документа
```

является сильным match.

---

# 30. Нельзя использовать простой FIFO

FIFO используется только как крайний fallback.

Например:

```text
документ A = 5 500
документ B = 27 200

оплаты:
4 500
27 200
1 000
```

правильно:

```text
27 200 → B
4 500 + 1 000 → A
```

а не механическое списание по хронологии.

---

# 31. Обратная хронология

При равных остальных признаках предпочтителен ближайший предшествующий документ.

Например:

```text
01.01 документ 10 000
05.01 документ 10 000
06.01 оплата 10 000
```

при отсутствии других различий:

```text
→ документ 05.01
```

---

# 32. Scoring

Пример:

```text
+200 прямая ссылка на конкретный документ
     + совпал остаток

+180 счет → документ по "Введен на основании"
     + совпала сумма

+170 ПКО → документ по "Введен на основании"
     + совпала сумма

+120 точное совпадение остатка

+100 точное совпадение полной суммы

+90  точная комбинация сумм

+70  прямая ссылка на документ, но сумма отличается

+60  связь через тот же счет

+40  тот же договор

-20  другой договор

+35  тот же вид расчетов

+30  товар ↔ товар / услуга ↔ услуга

+0..25 semantic score

+0..20 обратная хронология
```

Hard penalties:

```text
другой контрагент                         → запрет всегда
будущий документ для DEBT                 → запрет
реализация следующего квартала для ADVANCE → не кандидат этапа 10
```

Веса, пороги и лимиты поиска живут **только** в `config/scoring.yaml`. Файл обязателен: отсутствие — ошибка запуска (`FAILED`). В коде matcher второго набора весов нет. Хеш содержимого `scoring.yaml` пишется в `run_status.csv` и `analysis.log`. Значения ниже — содержимое этого файла, не подсказка «на усмотрение реализации».

---

# 32.1. Пороги уверенности и неоднозначность

Стартовые правила, меняются только конфигурацией, не хардкодом в matcher:

```text
HIGH:   score >= 150 И (нет второго кандидата ИЛИ отрыв >= 40)
MEDIUM: score >= 80  И отрыв >= 20
LOW:    score >= 50  И отрыв >= 10
иначе:  DEBT_UNRESOLVED
```

Дополнительно:

- результат почти только по хронологии не может быть `HIGH`;
- точная сумма даёт автоматическую блокировку только при единственном кандидате;
- в комбинаторный поиск не более 8 кандидатов на одну оплату;
- лимит времени поиска задаётся в конфигурации (старт: 2 секунды на контрагента);
- при превышении лимита — детерминированный fallback: этапы 3–6 уже заблокированного, остаток `DEBT_UNRESOLVED`, запись в `analysis.log`;
- пересмотр уже заблокированной связи `HIGH` запрещён; `LOW` можно пересмотреть, пока обрабатывается тот же контрагент и не начат следующий.

---

---

# 33. Парсер и эвристический matcher — разные компоненты

Не смешивать:

```text
"извлечь из текста счет №123"
```

и:

```text
"решить, какой документ фактически оплачивается"
```

Первое выполняет parser.

Второе выполняет matcher.

Это позволит использовать уже существующий parser без переписывания всей системы.

---

# 34. Hugging Face

Используется только после обычных признаков.

Рекомендуемый порядок:

```text
точные ссылки
→ суммы
→ связи оснований
→ договор/вид расчетов
→ обычный scoring
→ только затем semantic reranker
```

Модель не нужна для:

```text
"Введен на основании: Счет №123"
```

или:

```text
платеж 27 200
документ 27 200
тот же счет
```

---

# 35. Когда использовать модель

Например осталось:

```text
три старых документа
нет номера счета
нет прямой ссылки
нет точной суммы
договоры не помогают
```

Тогда можно сравнивать тексты:

```text
payment_text
document_text
```

через multilingual reranker.

---

# 36. Восстановление взаиморасчётов

Использовать `opening_balances.csv` и `ledger62.csv` с начала исторического периода.

По умолчанию:

```text
01.10 предыдущего года
```

Состояние = обязательное начальное сальдо + движения `ledger62.csv` с `ИсторияНач`.

Оба поля `ОстатокДтКоп` и `ОстатокКтКоп` используются. Нельзя схлопывать их правилом «если есть кредит — вернуть кредит».

Проводка оплаты применяется к взаиморасчётам **только** из `ledger62.csv`. Строка `payments.csv` — расширенное описание того же события, не второе движение денег. Несколько строк ledger с одним `payment_id` — части одной оплаты; каждая проводится один раз по своему `ledger_event_id`. Оплаты из `ledger62.csv` не отбрасывать.

Проводки реализации связываются с документами по `source_document_id`. Уникальность строки ledger — `ledger_event_id`.

`DocumentRole=INVOICE` не создаёт задолженность. Кандидаты погашения долга — `RECEIVABLE`, существовавшие на момент оплаты. Кандидаты этапа 10 — `RECEIVABLE` после оплаты до конца квартала этой оплаты. Счета остаются связкой в обоих окнах.

Перед matching Python читает `run_id` из `manifest.csv` и копирует его в `kudir_result.csv` и `run_status.csv`.

В `documents.csv` допустимы счета и реализации с датой после `ДатаКон`, но не позже `КонецКвартала(ДатаКон)`. Для восстановления долга на момент оплаты они не используются. Их читает только этап 10.

Поля `run_status.csv`:

```text
key;value
run_id;...
status;SUCCESS|SUCCESS_DEGRADED|FAILED
parser_available;0|1
schema_version;1
scoring_hash;...
```

---

# 37. Недостаточная историческая глубина

Если на `ИсторияНач` есть ненулевой остаток, который нельзя разложить на выгруженные документы, это не ошибка файла: конкретный старый акт вне истории не обязан быть найден.

Погашение такого остатка целевым платежом без однозначного документа:

```text
DEBT_UNRESOLVED
```

Отсутствие самого `opening_balances.csv` — ошибка обработки.

Snapshot документных остатков между запусками в версии 1 не используется.

---

# 38. opening_balances.csv

Обязательный вход. Минимальные поля — как в ТЗ №1:

```text
КонтрагентID
Счет62
ДоговорID
ВидРасчетовID
ВидРасчетов
ДатаСальдо
ОстатокДтКоп
ОстатокКтКоп
```

Тогда:

```text
начальное сальдо
+
движения с ИсторияНач
=
состояние на дату каждого платежа
```

Это восстанавливает размер долга, но не имя документа, созданного до `ИсторияНач`.

---

# 39. DEBT_UNRESOLVED

Если точно известно:

```text
платеж погашает долг
```

но конкретный документ надёжно определить невозможно:

```text
не превращать сумму в аванс.
```

Результат:

```text
DEBT_UNRESOLVED
```

---

# 40. Формирование строк результата

Типы:

```text
DEBT
ADVANCE
DEBT_UNRESOLVED
```

Одна исходная оплата может дать любое количество строк. В `kudir_result.csv` пишутся только части оплат с `is_target=1`.

Порядок колонок `kudir_result.csv` — как в ТЗ №1:

```text
run_id
result_row_id
payment_id
row_type
matched_document_id
contract_id
amount_kopecks
advance_kopecks
repayment_kopecks
income_kopecks
vat_base_kopecks
vat_kopecks
match_type
confidence
reason
СодержаниеЗаписи
```

`contract_id` для `DEBT` — договор найденного документа, для `ADVANCE` с реализацией — договор этой реализации, для `ADVANCE` без документа — договор оплаты. Объекты 1С в файл не записываются.

---

# 41. СуммаПлатежа

В результирующей строке:

```text
СуммаПлатежа
=
сумма конкретной части
```

Не полная сумма исходного банковского документа.

---

# 42. СуммаВАванс / СуммаВОпалату

Для `ADVANCE`:

```text
advance_kopecks = сумма части
repayment_kopecks = 0
```

Для `DEBT` и `DEBT_UNRESOLVED`:

```text
advance_kopecks = 0
repayment_kopecks = сумма части
```

---

# 43. СуммаВДоход (версия 1)

Заглушка matching, не налоговый доход УСН:

```text
income_kopecks = amount_kopecks
```

Результат нельзя считать готовой КУДиР.

---

# 44. НДС

В версии 1:

```text
vat_base_kopecks = 0
vat_kopecks = 0
```

Не вычитать 5/105 из банковского поступления. Не брать НДС из реализации. Это цикл 2: он сможет это сделать только если цикл 1 уже проставил `matched_document_id` у закрытого аванса и оставил пустым у незакрытого остатка.

---

# 45. СодержаниеЗаписи

Только подтверждённые реквизиты. Нет номера, даты или договора — этот фрагмент не подставляется.

Шаблоны:

```text
DEBT + оказание услуг:
Оплата от {Контрагент} за услуги по акту №{Номер} от {Дата}

DEBT + отгрузка:
Оплата от {Контрагент} за товар по накладной №{Номер} от {Дата}

DEBT_UNRESOLVED + договор:
Оплата задолженности от {Контрагент} по договору {Договор}

DEBT_UNRESOLVED без договора:
Оплата задолженности от {Контрагент}

ADVANCE + документ + отгрузка:
Получен аванс от {Контрагент} в счёт товара по накладной №{Номер} от {Дата}

ADVANCE + документ + оказание услуг:
Получен аванс от {Контрагент} в счёт услуг по акту №{Номер} от {Дата}

ADVANCE + договор без документа:
Получен аванс от {Контрагент} по договору {Договор}

ADVANCE без договора и без документа:
Получен аванс от {Контрагент}
```

Не выдумывать отсутствующие реквизиты.

---

# 46. matches.csv

Для каждой связи сохранять:

```text
payment_id
document_id

matched_amount_kopecks

match_type
score
confidence

direct_reference_score
invoice_link_score
amount_score
contract_score
calculation_type_score
semantic_score
chronology_score

reason
```

Например:

```text
INVOICE_BASE_EXACT+EXACT_REMAINDER+CROSS_CONTRACT_MATCH
```

---

# 47. Regression-тесты существующего parser

Обязательно сформировать тестовый набор из реальных строк `доходы-2`.

Например:

```text
Оплата по документу Счет ...
Оплата по счету ...
Оплата по сч ...
Оплата по акту ...
```

Зафиксировать текущий ожидаемый результат.

При переносе/рефакторинге parser'а все эти тесты должны оставаться зелёными.

---

# 48. Новые тесты

### Тест: прямой акт

```text
платеж содержит акт №25
акт = 5 500
платеж = 5 500
```

→ `DIRECT_DOCUMENT_EXACT`.

### Тест: акт указан, частичная оплата

```text
акт = 5 500
платеж = 4 500
уникальная ссылка на акт
```

→ `HIGH_PARTIAL_DIRECT`, связь фиксируется.

### Тест: через счет

```text
платеж:
Оплата по счету №00002

документ:
Оказание услуг №00005
Комментарий:
Введен на основании: Счет на оплату №00002 ...

суммы совпадают
```

→ `INVOICE_BASE_EXACT`.

### Тест: ПКО

```text
ПКО:
Комментарий:
Введен на основании: Отгрузка товаров №Н00570 ...

сумма совпадает
```

→ `CASH_BASE_EXACT`.

### Тест: разные договоры

```text
платеж — договор A
документ — договор B

но:
счет совпадает
основание совпадает
сумма совпадает
```

→ документ разрешено выбрать.

В `reason`:

```text
CROSS_CONTRACT_MATCH
```

### Тест: сумма важнее хронологии

```text
10 000 старый документ
12 000 новый документ
платеж 10 000
```

→ 10 000.

### Тест: один payment_id, несколько корреспонденций

```text
одна проводка выписки (один НомерПроводки)
две корреспонденции 62, разные виды расчётов
```

→ один `payment_id`, два `ledger_event_id`. Не фиксировать автоматически два разных документа только из-за двух видов расчётов.

### Тест: две оплаты в одной выписке без номера строки

```text
Выписка 0001, НомерПроводки=2, МЫЛОВА, 1 000
Выписка 0001, НомерПроводки=4, РЯБОВА, 1 950
НомерСтрокиДокумента = 0
```

→ два разных `payment_id`. Нельзя склеивать их из-за одной выписки.

### Тест: несколько оплат

```text
4 500 + 1 000 → 5 500
```

Ключевой golden-кейс `пример1`: январские 1 000 закрывают остаток акта услуг 5 500 после исторических 4 500. Исторические оплаты имеют `is_target=0` и в `kudir_result.csv` не попадают.

### Тест: две одинаковые суммы без прочих признаков

```text
два документа по 10 000
платёж 10 000
нет ссылки и нет отрыва
```

→ не автоматический HIGH; при недостаточном отрыве `DEBT_UNRESOLVED`.

### Тест: нет opening_balances.csv

→ ошибка обработки, не расчёт с нуля.

### Тест: аванс и отгрузка в том же квартале через счёт

```text
10 января оплата 105 000, долга нет
назначение: оплата по счету №00002
20 февраля отгрузка 105 000
комментарий: Введен на основании: Счет на оплату №00002 ...
```

→ `ADVANCE` с `matched_document_id` февральской отгрузки, `ADVANCE_INVOICE_BASE`.
Не оставлять аванс без документа только потому, что накладная позже оплаты.

### Тест: отгрузка в следующем квартале

```text
10 января аванс 105 000
05 апреля отгрузка 105 000, тот же счёт
```

→ `ADVANCE` без документа. Апрель — II квартал, этап 10 её не берёт.

### Тест: частичное закрытие аванса до конца квартала

```text
аванс 105 000
до 31 марта: реализация 42 000 без привязки НДС
             и реализация 21 000
остаток 42 000
```

→ две (или три) строки `ADVANCE` на найденные реализации и строка `ADVANCE` 42 000 без документа.
Сумма частей = 105 000. НДС не считать.

### Тест: долг не может ссылаться на будущий документ

```text
на момент оплаты есть долг
единственная «подходящая» накладная датирована после оплаты
```

→ `DEBT` / `DEBT_UNRESOLVED`, не привязка к будущей накладной.
Этап 10 не перекладывает эту сумму в аванс.

### Тест: порядок внутри одной даты

`Пример2`: 15 800 → Отгрузка товаров Н00001, 5 500 → Оказание услуг 00001, с учётом `ПозицияДокумента`.

---

# 48.1. CSV-протокол

Чтение и запись — строго по разделу 30 ТЗ №1: Windows-1251, разделитель `;`, даты `YYYY-MM-DD`, целые копейки, quoting с удвоением кавычек, пустое поле ≠ `0` и ≠ `NULL`.

При отсутствии `manifest.csv`, неизвестной или несовпадающей `schema_version` Python **немедленно** прекращает обработку до загрузки остальных файлов, не создаёт `kudir_result.csv` и возвращает ненулевой код (`FAILED`). Автоконвертация схемы не выполняется.

---

# 48.2. Golden-тесты

`пример1.csv` и `Пример2.csv` — исходные бухгалтерские факты, не вход нового модуля. При реализации они превращаются в комплекты:

```text
tests/golden/example1/
    manifest.csv
    payments.csv
    ledger62.csv
    documents.csv
    opening_balances.csv
    expected_kudir_result.csv
    expected_matches.csv
    expected_unresolved.csv
    expected_run_status.csv

tests/golden/example2/
    ... тот же набор ...
```

Имена полей — как в текущем контракте (`ledger_event_id`, не устаревший `event_id`).

Ожидания:

- `example1`: 06.01.2026, 1 000 → `DEBT` на оказание услуг Я-002374 (остаток после исторических 4 500); исторические оплаты `is_target=0` в результат не входят.
- `example2`: на одной дате 15 800 → Отгрузка товаров Н00001, 5 500 → Оказание услуг 00001, порядок по `ПозицияДокумента` / номеру строки.

До полной выгрузки 1С комплекты можно собирать вручную из фактов; без `expected_*.csv` тест не считается golden.

---

# 49. Критерии готовности Python

1. Существующий parser вызывается как внешний сервис и покрыт regression-тестами; код сервиса не копируется.
2. Поддерживается прямое указание акта/накладной, включая `HIGH_PARTIAL_DIRECT`.
3. Поддерживается оплата по счету и цепочка `Оплата → Счет → Реализация`, в том числе если реализация позже оплаты в том же квартале.
4. Парсятся стандартизированные комментарии `Введен на основании`.
5. Поддерживается ПКО.
6. При неединственной ссылке или равных суммах нескольких кандидатов автоматический HIGH не ставится.
7. Разный договор не является hard reject.
8. Другой контрагент является hard reject.
9. Будущий документ является hard reject для `DEBT` и кандидатом этапа 10 для `ADVANCE` до конца квартала оплаты.
10. Поддерживаются комбинации оплат и документов.
11. Matcher идёт этапами блокировки; FIFO только как последний fallback.
12. Пороги HIGH/MEDIUM/LOW и отрыв заданы конфигурацией.
13. ML является опциональным и в версии 1 не обязателен.
14. Формируется `kudir_result.csv` по схеме ТЗ №1 только для `is_target=1`, с тем же `run_id`.
15. Формируется `matches.csv`.
16. Поддерживается `DEBT_UNRESOLVED`, в том числе для долга до начала истории.
17. Контролируется сумма каждой исходной целевой оплаты.
18. `opening_balances.csv` обязателен; оба остатка Дт и Кт используются.
19. CSV читается по протоколу ТЗ №1 (Windows-1251, `;`, даты ISO, quoting).
20. Оплата склеивается с проводками 62 по `payment_id` и проводится только через ledger.
21. `config/scoring.yaml` обязателен и является единственным источником весов.
22. Недоступность parser даёт `SUCCESS_DEGRADED`, а не скрытый успех.
23. Версия 1 не претендует на готовый налоговый доход КУДиР и не считает НДС.
24. Несвязанный аванс закрывается реализациями того же квартала; реализация следующего квартала не привязывается.
25. Табличная часть счёта и ставки НДС товаров в версии 1 не разбираются.
