# Архитектура формирования КУДиР по оплатам покупателей

## 1. Статус документа

Этот документ описывает общую архитектуру и бизнес-логику проекта. Детальная реализация определяется двумя специализированными документами:

1. `01-ТЗ-1С-выгрузка-данных.md` — единственный источник требований к коду 1С 7.7.
2. `02-ТЗ-Python-анализ-оплат.md` — единственный источник требований к анализу и Python-коду.

При противоречии приоритет имеет соответствующее специализированное ТЗ. Здесь не фиксируются полные схемы CSV, конкретные имена новых процедур, веса scoring и внутренняя структура классов.

**Версия 1 — движок matching оплат, а не готовая КУДиР.** Поля НДС и окончательный налоговый доход не формируются. Результат нельзя считать книгой учёта доходов для сдачи. Налоговый слой (НДС 5%, возвраты, эквайринг, переходные оплаты) — отдельный последующий этап.

## 2. Цель

На основании оплат, проводок по счёту 62 и документов реализации определить:

- какая часть оплаты погашает существующую задолженность;
- какой документ или документы ею оплачены;
- какая часть является авансом;
- какая сумма относится к долгу, но не может быть надёжно связана с документом;
- какие строки версии 1 загружаются в 1С для дальнейшей работы существующей таблицы доходов.

Результат загружается в 1С и используется существующей функцией `СформироватьТаблицуДоходовЧистовая()`. Это техническая загрузка matching, а не утверждение, что сумма уже является налоговым доходом УСН.
## 3. Общая схема и ответственность

```text
1С 7.7 → CSV → Python: взаиморасчёты и matching
       ← CSV ← результаты и диагностика
       → СформироватьТаблицуДоходовЧистовая()
```

### 3.1. 1С 7.7

1С является источником бухгалтерских фактов и потребителем результата. Она должна:

- выгрузить все оплаты с `ИсторияНач` по `ДатаКон` с признаком `is_target`;
- для выписки считать самостоятельной оплатой одну проводку (`Опер.НомерПроводки()`), а не строку табличной части: в этой конфигурации нет `ПривязыватьСтроку`, `НомерСтрокиДокумента()` всегда 0;
- выгрузить движения по счёту 62; кредитовая оплата должна быть и в `payments.csv`, и в `ledger62.csv` с одним `payment_id`;
- выгрузить счета, отгрузки и оказания услуг анализируемых клиентов;
- передать контрагента, договор, вид расчётов, даты и позиции документов;
- передать содержание, представление, назначение платежа, первичный документ и комментарий;
- обязательно выгрузить начальное сальдо на `ИсторияНач` в `opening_balances.csv`;
- сохранить таблицу соответствия технических ID ссылкам 1С и по ней восстановить объекты при загрузке;
- загрузить итоговый CSV и сформировать существующую таблицу доходов.

1С не выполняет эвристический matching и не дублирует Python-алгоритм.

### 3.2. Python

Python является единственным местом, где выполняются:

- восстановление взаиморасчётов на момент каждой оплаты;
- разделение оплаты на погашение долга и аванс;
- разбор прямых и косвенных ссылок на документы;
- matching, поиск комбинаций и scoring;
- необязательный semantic reranking;
- формирование строк результата matching и диагностики.

Результат должен быть воспроизводимым и покрытым автоматическими тестами.
## 4. Контракт обмена

Входы Python:

| Файл | Назначение |
| --- | --- |
| `manifest.csv` | `run_id`, версия схемы, период, кодировка |
| `payments.csv` | Все оплаты с `ИсторияНач` по `ДатаКон`, включая исторические |
| `ledger62.csv` | Движения по счёту 62 в хронологическом порядке |
| `documents.csv` | Счета, отгрузки и оказания услуг |
| `opening_balances.csv` | Обязательное начальное сальдо на `ИсторияНач` |

Выходы Python:

| Файл | Назначение |
| --- | --- |
| `kudir_result.csv` | Строки matching для загрузки в 1С (только `is_target=1`) |
| `matches.csv` | Связи, оценки и объяснения |
| `unresolved.csv` | Неоднозначные и неразрешённые случаи |
| `analysis.log` | Ошибки данных и контроль инвариантов |
| `run_status.csv` | `SUCCESS` / `SUCCESS_DEGRADED` / `FAILED` |

Полные схемы, порядок колонок и CSV-протокол определяются специализированными ТЗ. Деньги хранятся целым количеством копеек. Каждая сущность имеет стабильный идентификатор, не зависящий от текстового представления.

Через CSV передаются только ID и скалярные поля. Объекты 1С (`ДокументОплаты`, `Договор` и т.п.) в CSV не сериализуются: Python возвращает `payment_id`, `matched_document_id` и `contract_id`, 1С восстанавливает ссылки по таблице соответствия, построенной при выгрузке.

Оплата — две проекции одного события: одна строка `payments.csv` и одна или несколько кредитовых корреспонденций `ledger62.csv` с тем же `payment_id`. Ключ оплаты выписки — документ + `НомерПроводки()`, не номер строки табличной части. К взаиморасчётам применяется только ledger. `payments.csv` не второе движение денег. Несколько корреспонденций одной проводки не означают автоматически несколько оплачиваемых документов. Порядок: `ДатаОперации + ПозицияДокумента + НомерПроводкиВДокументе`. `run_id` живёт в `manifest.csv` и копируется в результат. Веса matching — только `config/scoring.yaml`.

## 5. История, долг и аванс

История по умолчанию начинается 1 октября года, предшествующего отчётному. Параметр `МесяцевИстории` изменяемый. `opening_balances.csv` обязателен во всех трёх документах проекта: без него нельзя объяснить долг, возникший раньше начала выгрузки. Отсутствие файла — ошибка обработки, а не молчаливый старт с нуля.

`payments.csv` содержит все оплаты с `ИсторияНач` по `ДатаКон`, а не только платежи отчётного периода. Признак `is_target`:

- `1` — оплата попадает в результат версии 1 (`ДатаНач`…`ДатаКон`);
- `0` — историческая оплата, нужна только чтобы восстановить документные остатки.

Python прогоняет matching по всем оплатам. Строки `kudir_result.csv` формируются только для `is_target=1`.

Документы и оплаты ищутся только внутри выбранной глубины истории. Snapshot документных остатков между запусками в версии 1 не используется. Если долг возник раньше `ИсторияНач` и конкретный документ установить невозможно, формируется `DEBT_UNRESOLVED`.

Состояние на дату оплаты рассчитывается последовательно:

```text
начальное сальдо + движения с начала периода
```

Несогласованное начальное состояние или разрыв движений отражается в диагностике, а не исправляется молча.

Разделение оплаты выполняется бухгалтерской арифметикой до поиска документа:

```text
debt_part    = min(сумма оплаты, долг перед оплатой)
advance_part = сумма оплаты - debt_part
```

Например, долг 7 000 и оплата 10 000 дают `DEBT = 7 000` и `ADVANCE = 3 000`. ML/LLM в этом расчёте не участвует.

Если погашение долга установлено, но документ надёжно не найден, сумма получает тип `DEBT_UNRESOLVED`, а не превращается в аванс.

## 6. Существующий parser

Parser является внешней зависимостью и в этот репозиторий не входит. Новый parser в рамках проекта не разрабатывается и не копируется.

В `доходы-2.txt` уже используется `РазобратьНазначениеПлатежа(...)`, обращающийся к локальному сервису `http://127.0.0.1:8765/parse1c`. Механизм извлекает смысл платежа, тип, номер и дату упомянутого документа.

1С-клиент этого сервиса подключается через адаптер. Python вызывает тот же сервис. Regression-тесты: сохранённые ответы (unit) и живой сервис (integration, можно пропускать). Если сервис недоступен, разбор текста пропускается, matching продолжается, итог запуска — `SUCCESS_DEGRADED`; 1С показывает предупреждение.

Parser отвечает на вопрос «что написано в тексте»; matcher — «какой документ фактически оплачивается». Эти задачи не смешиваются.

Путь к сервису, команда запуска и формат запроса/ответа указываются в ТЗ №2. Git-submodule для внешних инструментов не используется.

## 7. Порядок matching

Matcher работает этапами блокировки. Раннее слабое решение не должно разрушать более сильное точное соответствие. Простой greedy и FIFO запрещены как основной алгоритм.

Этапы:

1. восстановить долг и остатки на момент оплаты;
2. заблокировать уникальные машинные ссылки (`Введен на основании`, ПКО);
3. зафиксировать уникальные прямые ссылки, включая допустимые частичные оплаты;
4. зафиксировать уникальные совпадения через счёт;
5. зафиксировать уникальные совпадения с остатком документа;
6. решить оставшиеся конфликты внутри одного контрагента ограниченным поиском;
7. всё ниже порога оставить `DEBT_UNRESOLVED`;
8. semantic-модель в версии 1 не обязательна и подключается только после реальной статистики неоднозначностей;
9. обратная хронология — только последний fallback при равенстве более сильных признаков.

### 7.1. Прямая ссылка

Уникальная машинная или разобранная ссылка на документ того же контрагента плюс условие `0 < сумма платежа (или его долговой части) <= остаток документа` даёт связь высокой уверенности `HIGH_PARTIAL_DIRECT` или `DIRECT_DOCUMENT_EXACT` при полном закрытии остатка.

Если платёж больше остатка указанного документа: остаток относится на этот документ, хвост идёт в дальнейший matching или в `ADVANCE`.

Точная сумма считается сильным признаком только при единственном подходящем кандидате. Два документа по 10 000 и платёж 10 000 сами по себе однозначной связи не создают.

### 7.2. Оплата по счёту

Счёт не является документом задолженности. Необходимо построить цепочку:

```text
платёж → счёт → отгрузка товаров / оказание услуг
```

Связь счёта с реализацией определяется структурированным разбором комментария `Введен на основании`, а не посимвольным сравнением строки. Если одному счёту соответствуют несколько реализаций, проверяются суммы, остатки и комбинации; первый найденный документ автоматически не выбирается.

### 7.3. ПКО

Для наличной оплаты сначала анализируется комментарий ПКО. Машинная ссылка на реализацию плюс `0 < сумма <= остаток` даёт связь высокой уверенности, в том числе частичную. Если сумма больше остатка, остаток идёт на указанный документ, хвост — дальше.

## 8. Кандидаты и ограничения

Документ может быть кандидатом, только если он относится к тому же контрагенту, существовал на момент оплаты и имеет непогашенный остаток.

Другой контрагент и будущий документ — жёсткие запреты. Для документов с одной датой используется `ПозицияДокумента`, а не случайный порядок строк.

Договор не является жёстким ограничением:

- совпадение договора даёт сильный плюс;
- несовпадение даёт штраф, но не запрещает связь;
- машинная цепочка и точная сумма могут перебить несовпадение;
- такой результат помечается `CROSS_CONTRACT_MATCH`.

В строке `DEBT` возвращается договор найденного документа задолженности; в строке `ADVANCE` — договор оплаты.
## 9. Суммы, комбинации и конфликты

Сильными считаются следующие случаи:

- оплата равна текущему остатку документа;
- оплата равна полной сумме ещё не оплаченного документа;
- несколько оплат вместе закрывают один документ;
- одна оплата закрывает несколько документов;
- очередная оплата точно закрывает остаток после частичной оплаты.

Связь является many-to-many: одна оплата может породить несколько строк результата, а один документ может закрываться несколькими оплатами.

Простой greedy или FIFO запрещён как основной алгоритм. Раннее слабое решение не должно разрушать более сильное точное соответствие. Конфликты внутри одного контрагента решаются этапами блокировки из раздела 7, а не скрытым перебором «на усмотрение реализации». Ограниченный комбинаторный поиск допускается только после уникальных блокировок, с лимитом кандидатов и детерминированным fallback, заданными в ТЗ №2.

Обратная хронология предпочитает ближайший предшествующий документ только при равенстве более сильных признаков.

## 10. Scoring, уверенность и ML

Scoring учитывает, в порядке убывания значимости:

1. прямую ссылку на документ;
2. цепочку через счёт или основание;
3. совпадение с остатком и полной суммой;
4. точную комбинацию сумм;
5. договор и вид расчётов;
6. соответствие «товар/услуга»;
7. текстовую семантику;
8. близость по времени.

Веса, пороги `HIGH` / `MEDIUM` / `LOW`, минимальный отрыв от второго кандидата и условие перехода в `DEBT_UNRESOLVED` находятся в конфигурации Python и ТЗ №2. Каждая связь получает `match_type`, числовой `score`, `confidence` и машиночитаемый `reason`. Результат почти только по хронологии не может иметь `HIGH`.

Semantic reranker запускается только для оставшихся неоднозначных кандидатов. Он сравнивает тексты, но не рассчитывает остатки, не распределяет деньги и не отменяет жёсткие ограничения. При недоступности модели система продолжает работу; первая версия должна быть возможна без ML.
## 11. Результат matching (версия 1)

Допустимые типы строки:

- `DEBT` — погашение долга с найденным документом;
- `ADVANCE` — полученный аванс;
- `DEBT_UNRESOLVED` — погашение долга без надёжной привязки.

В каждой строке сумма равна конкретной части, а не полной сумме исходной оплаты.

- Для `ADVANCE`: аванс = сумма части, в оплату = 0.
- Для `DEBT` и `DEBT_UNRESOLVED`: аванс = 0, в оплату = сумма части.
- В версии 1: доход = сумма части, база НДС = 0, НДС = 0. Это заглушка matching, а не налоговый расчёт.

`kudir_result.csv` содержит `run_id`, ID и скаляры (`payment_id`, `matched_document_id`, `contract_id`, суммы в копейках, `match_type`, `confidence`, `reason`, текст содержания). 1С восстанавливает `ДокументОплаты` и `Договор` по таблице соответствия.

`СодержаниеЗаписи` формируется только из подтверждённых реквизитов. Отсутствующие номер, дата или договор не выдумываются.

## 12. Инварианты и диагностика

Для каждой исходной оплаты обязательно:

```text
сумма DEBT + сумма ADVANCE + сумма DEBT_UNRESOLVED
= сумма исходной оплаты
```

Сумма привязок к документу не превышает его доступный остаток. Остатки оплаты и документа не могут быть отрицательными. Одинаковые входные данные и настройки всегда дают одинаковый результат.

Нарушение инварианта является ошибкой обработки, а не низкой уверенностью. `matches.csv` должен объяснять каждую связь; неоднозначные случаи сохраняются отдельно для ручной проверки.

Минимальные автоматические сценарии:

- прямая ссылка с совпадающей и несовпадающей суммой;
- оплата по счёту с переходом к реализации;
- ПКО со ссылкой на основание;
- сильная связь при разных договорах;
- точная сумма сильнее хронологии;
- несколько оплат ↔ один документ и одна оплата ↔ несколько документов;
- закрытие остатка после частичной оплаты;
- отсутствие `opening_balances.csv` — ошибка, а не расчёт с нуля;
- исторический платёж (`is_target=0`) восстанавливает остаток, но не попадает в `kudir_result.csv`;
- недоступность parser-сервиса и semantic-модели;
- сохранение полной суммы каждой исходной оплаты.

Regression-набор parser формируется из реальных строк `доходы-2.txt` и должен оставаться зелёным при переносе или рефакторинге.
## 13. Критерии архитектурной готовности

1. 1С выгружает полные исходные факты, обязательное начальное сальдо и не содержит второго matcher.
2. Исторические оплаты выгружаются с `is_target`; в результат версии 1 попадают только целевые.
3. Python точно восстанавливает долг и аванс на дату оплаты.
4. Существующий внешний parser переиспользован и защищён regression-тестами; в репозиторий не копируется.
5. Поддержаны прямые ссылки, частичные оплаты по прямой ссылке, счета, документы-основания и ПКО.
6. Работают many-to-many связи и закрытие остатков.
7. Другой контрагент и будущий документ запрещены.
8. Разный договор допускается как диагностируемое исключение.
9. FIFO используется только как fallback после этапов блокировки.
10. ML остаётся необязательным дополнительным признаком.
11. Каждая сумма сохранена, каждое решение объяснимо, CSV несёт ID, а не объекты 1С.
12. Версия 1 явно не претендует на готовый налоговый доход КУДиР.

## 14. CSV-протокол (обязательные правила)

Детали колонок — в специализированных ТЗ. Общие правила одинаковы для всех файлов:

- `run_id` и `schema_version=1` в `manifest.csv`;
- кодировка Windows-1251;
- разделитель `;`;
- даты строго `YYYY-MM-DD`;
- суммы — целые копейки;
- переносы строк внутри поля только в кавычках;
- кавычка внутри поля удваивается;
- пустое значение — пустая строка, не `NULL` и не `0`;
- точные имена и порядок колонок из ТЗ №1.

## 15. Связанные материалы

- `01-ТЗ-1С-выгрузка-данных.md` — форматы выгрузки/загрузки и код 1С.
- `02-ТЗ-Python-анализ-оплат.md` — алгоритмы, веса, модули и тесты Python.
- `доходы-2.txt` — существующее формирование КУДиР и интеграция с parser.
- `Анализ-взаирасчетов.txt` — существующий код работы с взаиморасчётами.
- `пример1.csv`, `Пример2.csv` — исходные факты; структура golden-комплектов — в ТЗ №2, раздел 48.2.

Старый `ТЗ.txt` после принятия этого документа считается архивным и не является источником требований.
